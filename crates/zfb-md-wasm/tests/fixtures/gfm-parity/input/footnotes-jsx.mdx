---
title: Footnotes In JSX
---

# Heading

<Callout>
  A footnote nested inside an MDX JSX element body[^nested] still numbers by
  first-reference order.

[^nested]:
    This definition is itself nested inside the same JSX element
    body, not the document's top level.

</Callout>
