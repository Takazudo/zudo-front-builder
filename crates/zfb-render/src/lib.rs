//! zfb-render: file-based router + JSX layout rendering.
//!
//! NOTE (Sub 6 stub): This `lib.rs` is a minimal stub that exposes only
//! `pub mod meta;` so that `meta.rs` compiles in isolation. Sub 3 owns
//! the real crate skeleton and will replace this file at merge.

pub mod meta;
