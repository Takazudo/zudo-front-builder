//! zfb-render: rendering engine for zfb (file-based router + JSX rendering).
//!
//! NOTE: This crate skeleton is a temporary local stub created by Sub 5 so that
//! the `paths.rs` module can compile and run unit tests in isolation. Sub 3
//! owns the full skeleton (with adapters, RenderHost trait, SWC pipeline, etc.)
//! and will replace this file at merge time.
pub mod paths;
