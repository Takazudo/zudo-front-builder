---
title: Seed
---

Seed post — unused by this fixture's own assertions. It exists only so
`content/posts/` is a real, git-tracked directory the watcher-live
handshake can write fresh-named warmup markers into (mirrors
`dev-loop-basic`'s fixture shape).
