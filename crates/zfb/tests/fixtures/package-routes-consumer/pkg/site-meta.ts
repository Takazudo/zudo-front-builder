/**
 * Shared metadata imported by both package route pages. Exercises relative
 * imports within the package — the overlay must resolve these correctly
 * (Z1a regression guard: `package_route_with_relative_import_bundles`).
 */
export const siteTitle = "Consumer Demo";
