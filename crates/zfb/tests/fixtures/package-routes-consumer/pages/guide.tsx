/**
 * Consumer project's own guide page. Verifies that multiple user pages/
 * routes are all unaffected when a preset injects additional routes.
 */
export default function GuidePage() {
  return (
    <html lang="en">
      <head>
        <title>Consumer Guide</title>
      </head>
      <body>
        <h1>CONSUMER_USER_GUIDE_MARKER</h1>
      </body>
    </html>
  );
}
