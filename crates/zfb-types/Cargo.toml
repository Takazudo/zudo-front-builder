[package]
name = "zfb-types"
version = "0.0.0"
edition = "2021"
license = "MIT"
description = "zfb-types: canonical shared data types used across multiple zfb crates"

[dependencies]
serde = { workspace = true }
sha2 = "0.10"
hex = "0.4"

[dev-dependencies]
tempfile = { workspace = true }
