[package]
name = "zfb-router"
version = "0.0.0"
edition = "2021"
license = "MIT"
description = "zfb router: scans pages directories and builds a sorted file-based route tree"

[lib]

[dependencies]
serde = { workspace = true }
thiserror = { workspace = true }
anyhow = { workspace = true }
walkdir = { workspace = true }
