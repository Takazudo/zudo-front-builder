{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Probe-only capability set: minimal core surface, plus shell.execute for spawning the zfb sidecar.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "shell:allow-execute"
  ]
}
