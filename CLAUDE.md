# zfb / zudo-front-builder

Workspace for the `zfb` Rust workspace + `@takazudo/zfb-runtime` and `zfb` npm packages.

## /x-wt-teams epic workflow rule

For each `[Epic]` issue in this repo (e.g. issues #52, #53, and the rest of the super-epic's zfb-side epics), the epic PR is independent and safe to merge into `main` as soon as its workflow completes — siblings do not stack on one base. So:

- **Always invoke `/x-wt-teams` with `-a` / `--auto`** for an epic in this repo (in addition to whatever other flags the user passes — `-gcoc`, `--stay`, etc.). `-a` runs `/pr-complete -c -w` after Step 15, which merges the root PR, closes the linked issue, and watches post-merge CI on `main`.
- **After the merge succeeds**, the workflow's auto-suggest step prints the next epic's `/x-wt-teams` command for the user to copy-paste. Do not start the next epic in the same session.
- If the user explicitly says "do NOT auto-merge" or passes a flag that conflicts with `-a`, defer to the user.
- This rule is specific to `[Epic]` issues. Non-epic issues (one-off bug fixes, follow-ups like #58 inside an open epic PR) keep the default behaviour — leave the PR open for the user to review.

## Worktree push policy (enforced)

This repo uses `/x-wt-teams` for multi-topic development. Child agents work in git worktrees under `worktrees/`. **Pushing from a worktree is forbidden.** Only the manager session — running from the main repo at the repo root — pushes, after merging topic branches into the base branch locally.

### Why

- CI runs on every push. Children pushing pre-empt the manager's merge + review step, multiplying CI cost across intermediate state.
- Topic branches in `worktrees/*/` are intermediate by design — they shouldn't appear as standalone PRs unless the manager creates them.

### How it's enforced

`.git/hooks/pre-push` is a direct script (not managed via `lefthook.yml`) that blocks any push from a git worktree. It is auto-installed by `pnpm install` (via the `prepare` lifecycle script) and can be re-installed manually with:

```sh
pnpm init-worktree
```

The installer source lives at `scripts/install-git-hooks.sh`; the hook itself at `scripts/hooks/pre-push`.

### Emergency bypass (human use)

```sh
ALLOW_WORKTREE_PUSH=1 git push ...
```

Use only when you genuinely need to push from a worktree (rare). Never set this in agent prompts.

### Guidance for agents

- **Child agents working in `worktrees/*/`:** commit locally only. Pushing will fail with the message above — do not retry, do not invoke the bypass. Report back to the manager with the branch name and commit SHAs; the manager merges and pushes from the main repo.
- **`/x-wt-teams` manager session:** the hook does not affect you. Your `git push` runs from the main repo (the cwd is the repo root, not `worktrees/...`). After every wave's local merges, push as usual. Do not pass `ALLOW_WORKTREE_PUSH` to children.
- **Remove each topic worktree right after its wave merges** (`git worktree remove worktrees/<topic>` once the merge is pushed and reviews are settled — re-verify the worktree is clean first). Every worktree accumulates its own `target/` (6–30G on this Rust workspace); leaving three merged worktrees around filled the disk mid-epic during #1670 (2026-07). Do not delete a worktree with uncommitted changes — surface it instead.

## Testing

zfb follows the **zudo-test-wisdom** strategy (the full guide: <https://takazudomodular.com/pj/zudo-test>). This section is the zfb-adapted, agent-facing summary — read it before writing or fixing tests. Every test sits on **two axes**:

- **Level** = *what a test can see* (logic → DOM → build output → browser → pixels).
- **Tier** = *where and when it runs* (inner loop → PR gate → scheduled → local heavy lane).

The axes are independent. "Too heavy for the PR gate" is a **tier** question, never a reason to rewrite a test at a lower **level**.

### Test levels (escalation ladder)

| Level | What it sees | In zfb |
|---|---|---|
| 1 — Unit/logic | pure functions, transforms | `cargo test` unit tests; vitest unit tests |
| 2 — DOM component | DOM structure, no CSS engine | `zfb-runtime` happy-dom vitest (the client router) |
| 3 — Build output | emitted files **and emitted code that runs** | reading built `dist/`; the *dynamic-import-the-emitted-`_worker.js`* pattern in `zfb-adapter-cloudflare` (zfb is a code generator — string-equality proves the template was written, executing the artifact proves it threads `env`/`ctx`) |
| 4 — E2E browser | real process / browser | `crates/zfb/tests/dev_serve_e2e.rs` (dev server, Rust process-level e2e); `pnpm test:router-chromium` (`tests/router-chromium/`, real Chromium via Playwright — CI-gated by `.github/workflows/router-chromium.yml`, path-filtered to `packages/zfb-runtime/**` + `packages/zfb/src/runtime.ts` (the islands runtime — #1399) so it is NOT a required check); `pnpm test:webkit-back` (`tests/webkit-back-history/`, real WebKit back/forward-cache nav via Playwright — T4 local-heavy, Mac only, never runs in CI; see the release-day hook in RELEASE_DAY_CHECKLIST.md) |
| 5 — Visual | computed styles + pixels | the docs site / any UI — use `/verify-ui` + `/headless-browser` |
| 6 — AI-based | final resort, **not for CI** | not used; only for canvas/zoom surfaces L4+L5 cannot reach |

**Escalation rule:** when a test passes but the problem persists, **escalate to the next level — do not re-run the same test, and never tell the user to "clear cache."** If it's still broken, the code is still broken. Any CSS/layout/visibility work defaults to **Level 5**.

### Execution tiers

| Tier | What it is in zfb | Status |
|---|---|---|
| **T0 — inner loop** | `cargo check`/`clippy` + scoped `cargo test -p <crate>` (affected), `pnpm -r --if-present typecheck`, `pnpm -r test`. Retries 0. | run constantly while coding |
| **T1 — PR gate** | `.github/workflows/health.yml` — fmt, `clippy -D warnings`, build, `cargo nextest run --workspace --profile ci` + a separate `cargo test --workspace --doc`, `pnpm -r test`, `format:check`, actionlint, build-no-v8. | **the authoritative gate.** A PR is mergeable when its required checks are green |
| **T3 — scheduled re-exam** | heavy/platform-bound lanes on a schedule | **Thin pre-release lane is LIVE; the full nightly upgrade is still deferred.** `.github/workflows/exam.yml` (issue #1344, weekly Saturday 04:17 UTC) runs the `#[ignore]`d env-gate + heavy manifest below allowed-to-fail, plus a full `cargo nextest run --workspace` + doctest re-exam on macOS (the one FSEvents-vs-inotify coverage gap `health.yml` can't cover, since it's ubuntu-only). `.github/workflows/drift-net.yml` (issue #1342, weekly Wednesday 03:43 UTC) re-runs the release clean-room smoke against the live `next` dist-tag on ubuntu + macOS between releases. `.github/workflows/security-audit.yml` (weekly Monday 07:17 UTC) joined the same tracked-lane pattern in issue #1394 — its `pnpm audit (prod)` and `cargo deny` jobs previously had no failure paper-trail at all. None of the three gates a PR (schedule + `workflow_dispatch` only); all file/close one deduped tracking issue per workflow (`scripts/file-exam-issue.sh`) and IFTTT-notify on failure. See "T3 cutover manifest" below for exactly what upgrades (nightly cadence, Windows leg, etc.) and its trigger |
| **T4 — local heavy lane** | `pnpm b4push` (below) | convenience, not enforcement |

**Do not scaffold unused tiers.** zfb needs T0 + T1 now; T4 is the new `b4push`; T3 has a thin weekly stand-in (`exam.yml` + `drift-net.yml`) — the full nightly upgrade stays documented-but-deferred until cutover (see the manifest below).

### Branch ruleset on `main` (T1 enforcement)

T1's "mergeable when required checks are green" is now enforced by a GitHub **ruleset** on `main` (id `18452968`, `main-required-status-checks`; created via `scripts/apply-main-ruleset.sh`, checked in for reproducibility — rerun it to recreate/update if the ruleset is ever deleted). It requires the `required_status_checks` rule with only the checks that run on **every** PR unconditionally (no path filters, no tag/workflow_run-only triggers): `health`, `build (no-v8)`, `Build binary (x86_64-unknown-linux-gnu)`, `Build binary (aarch64-unknown-linux-gnu)`, the 4 `Smoke * (local mode)` jobs, `Scaffold E2E (packed tarballs, pre-publish)`, and `pnpm audit (prod)`. (The two `Build binary` checks used to both render as the ambiguous `Build binary (ubuntu-22.04)` since both matrix legs share that runner — the job's `name:` is now keyed on `matrix.platform.target` instead so each leg has its own unique check context.) `Scaffold E2E (packed tarballs, pre-publish)` (issue #1345) runs unconditionally on every PR too (no path filter, no `if:` condition — it only depends on `build-binary`, which itself gates on `pull_request`/`push`, the same trigger scope as the rest of node-free-smoke.yml), so it belongs in this list. `Build docs site` is deliberately excluded because a sibling change (#1336) makes it path-filtered to `docs/**` — a required check that stops running on non-docs PRs would hang them forever. A `RepositoryRole` (`admin`, `actor_id: 5`) bypass actor with `bypass_mode: always` is configured so `/l-make-release`'s direct version-bump push to `main` keeps working — `required_status_checks` also blocks direct pushes (a directly-pushed commit has no passing checks recorded against it), so without this bypass entry releases would break. **Pending manual verification:** the bypass actor has not yet been proven with a live test push (see issue #1333 / the PR that introduced this ruleset for the exact verification command) — treat `/l-make-release`'s direct-push step as unverified against this ruleset until a repo owner confirms it.

**`base/**` epic-PRs now run the T1 gate too (issue #2076).** `health.yml` and `pr-checks.yml`'s `on.pull_request.branches` both widened from `[main]`-only to include `"base/**"` (matching the pattern/quoting `actionlint.yml`/`router-chromium.yml` already use, though those two are path-filtered and this change is not). Previously an `/x-wt-teams` epic-PR — which targets a super-epic `base/**` branch, never `main` directly — never triggered `health` or the required `pnpm audit (prod)` check at all; only the eventual main-targeting super-PR did, leaving every epic-PR's own review window unguarded by T1. Deliberately **`pull_request`-only**: `health.yml`'s `push:` trigger stays main-only (adding `base/**` there would double-run the full T1 suite — once for the epic-PR's `pull_request` event, again for every commit's `push` event to the same branch), and `pr-checks.yml` still carries no `push:` trigger at all. **CI-cost tradeoff, accepted deliberately:** a sweep with N sibling epics now pays for N full `health` + `pnpm audit (prod)` runs against `base/**` (one per epic-PR) on top of the one eventual run against `main` when the super-PR lands — more total CI minutes per sweep, in exchange for catching a broken epic before it merges into the shared super-epic base instead of only at the very end. `scripts/apply-main-ruleset.sh` and the ruleset it applies are unaffected — the ruleset's `required_status_checks` rule targets only `refs/heads/main`, so it neither gates nor is bypassed by a `base/**` PR's checks.

### Required behavior (agents)

1. **Declare the test plan first** — *what* you're testing, *which level*, *why* that level.
2. **Match level to goal** — don't verify a Level-5 visual bug with a Level-1 logic test.
3. **Escalate when a lower level passes but the problem persists** (never re-run, never "clear cache").
4. **Default to Level 5 for any UI/CSS/visibility work.**
5. **Report what was NOT tested** — state the blind spots.
6. **Verification specs don't self-graduate.** A one-time "it was done" proof is tagged `#[ignore = "verification: <why>"]` (Rust) / `@verification` (TS) and excluded from gates. Propose promotion to a tier in the PR description; never self-promote.
7. **Red checks block the author.** Any red check on a PR you authored blocks you, *even if it is not a required check* — the only exception is a test carrying a `flaky: <issue-url>` quarantine tag (Rust `#[ignore = "flaky: <url>"]`; TS `// flaky: <url>` above `it.skip(...)`, see the TS-side idiom below) with a linked open issue.
8. **Never game the gate.** Do not add `#[ignore]` / `test.skip`, a flaky tag, a loosened tolerance, or a deleted assertion **without a linked open issue**. Making a gate pass by editing existing assertions needs a fresh-context review — not the same session that wrote the change.
9. **Scoped heavy verification.** When a change touches code covered only by a heavy/quarantined lane, run those tests on a capable host before declaring the work done.

### Flaky tests

zfb hits flakiness often (mostly **Rust timing/ordering** in the dev-server, SSE, and plugin-runner paths). Handle it as a pipeline, not a shrug.

- **Retry budget:** local **0**; CI **1–2** with artifacts. **Pass-on-retry is a triage signal, not a success** — record it and schedule the fix. **More than 2 retries is a smell.**
- **The `flaky:` quarantine pipeline (Rust):**
  - **Step 0 — prove it ever genuinely passed** (not pass-by-skip) on some host. A test that can pass *nowhere* is **broken, not flaky** → fix or delete it now, no quarantine.
  - **Quarantine requires a paper trail** — mark it `#[ignore = "flaky: <issue-url>"]` (the inline issue URL is mandatory). `cargo test` skips `#[ignore]`d tests, so it no longer blocks the gate.
  - **It must still run somewhere, allowed-to-fail** — locally via `cargo test -- --ignored` (or `--include-ignored`), AND weekly in CI: `.github/workflows/exam.yml`'s `quarantine-heavy` job runs the exact env-gate + heavy subset of the manifest below (`--run-ignored ignored-only` with an exact-name filterset; `pending-feature`-tagged tests are deliberately excluded — they're blocked on unimplemented features, not flakiness) allowed-to-fail, filing/closing a deduped tracking issue so failure data keeps flowing without gating a PR. This is the thin pre-release stand-in (issue #1344) — see "T3 cutover manifest" below for what the full nightly upgrade still needs.
  - **Quarantine has an exit with a deadline — fix, demote, or delete.** It is not a parking lot. **Quarantine suspends *product* coverage, not just test coverage:** the behavior is unguarded until the test is fixed.
- **The 5 deflaking root causes** (fix the cause, don't add a sleep): bare timing waits, `networkidle` on no-request SPA navs, animations in flight, shared/order-coupled state, hydration races. zfb's Rust flakes are mostly timing/ordering — prefer event/condition-keyed waits and deterministic scheduling over fixed `sleep`s (as `zfb-test-utils`'s SSE helpers and the `plugin_runner` timeouts already do).
- **`cargo-nextest` (ADOPTED — issue #1340):** CI runs the Rust test suite under `cargo nextest run --workspace --profile ci` (health.yml), configured in `.config/nextest.toml`. This gives the CI retry budget, per-test timeouts, and JUnit output — the mechanisms below are now real, not aspirational.
  - **Retries are RECORDED, not hidden.** The `ci` profile sets `retries = 1`; a pass-on-retry is reported as a terminal `FLAKY` line and in the JUnit report (`target/nextest/ci/junit.xml`, uploaded as the `nextest-junit-ci` artifact). This is the mechanical enforcement of "**pass-on-retry is a triage signal, not a success**" — a future sub-task (#1341) consumes that telemetry to file/triage flakes. Do not raise `retries` to paper over a flake. Local runs (b4push `B4PUSH_FULL=1`) use nextest's **default** profile (`retries = 0`) so a local flake fails loudly.
  - **Doctests are NOT run by nextest.** health.yml keeps a **separate** `cargo test --workspace --doc` step. Current doctest baseline (verify before changing; do not trust stale counts): **5 doctests execute + pass** — `zfb-css` (2) and `zfb-islands` (3) — plus **8** example blocks the harness lists but marks ignored/`no_run` (`zfb-islands` 3, `zfb-server` 2, `zfb-test-utils` 1, `zfb-render` 2), i.e. **13 doctest entries across 5 crates**. (The old "~15 crates" figure in this doc was wrong; #1340 measured the real numbers.) Neither `zfb-render` entry is hand-written: every `deno_core::extension!` emits a `rust,ignore` usage example into the generated struct's docs, and the crate now registers two — `embedded_v8::fetch::zfb_fetch` (issue #2015) and `embedded_v8::crypto::zfb_crypto` (issue #2017). Measured directly after #2017: `cargo test -p zfb-render --doc` reports `0 passed; 0 failed; 2 ignored` (it was `1 ignored` before). A `cfg(test)`-only `extension!` adds no doctest entry. The doc-test step must not regress below this.
  - **Heavy e2e binaries are serialized by a nextest `test-group`.** nextest schedules test *binaries* concurrently by default, which would boot several real `zfb dev`/`zfb build`/`zfb preview` V8+esbuild processes at once. `.config/nextest.toml` defines a `[test-groups.e2e-heavy]` with `max-threads = 1` and an override assigning **30** heavy binaries to it (re-measured during issue #2029's confirm pass — the previous "29" figure had drifted stale, predating `gfm_footnotes_tasklists_confirm_build`): **18 flock-adopting** binaries (`build_terminates`, `client_bundling_cross_pipeline`, `cold_rewrite_prewarm_e2e`, `dev_bind_before_walk_e2e`, `dev_build_static_parity`, `dev_content_aggregate_cold_boot_e2e`, `dev_dep_invalidation_1284_e2e`, `dev_out_of_root_collection_e2e`, `dev_public_large_tree_smoke_e2e`, `dev_serve_e2e`, `dev_serve_injected_routes_e2e`, `dev_sibling_watch_1678_e2e`, `embedded_host_request_time_e2e`, `mirror_css_scan_mdx_e2e`, `page_extension_full_matrix_e2e`, `preview_cross_mode_e2e`, `ssr_reload_self_heal_e2e`, `wasm_ssr_dev_smoke_e2e`) that spawn `zfb dev` and/or `zfb build` and/or `zfb preview`, plus **12 build-only** binaries (`build_cleans_outdir`, `build_package_routes`, `build_package_routes_consumer`, `client_router_autoinclude_build`, `content_snapshot_no_deferred`, `css_modules_components_build`, `end_to_end_basic_blog_build`, `gfm_footnotes_tasklists_confirm_build`, `html_minify_build`, `page_extension_route_table_build`, `sibling_css_module_command_layer_build`, `wasm_ssr_adapter_e2e`) that do not adopt the flock. For the first 18, the flock and test-group are **defense in depth** (flock = process-level guarantee; test-group = runner-level guarantee); for the build-only 12, the test-group is the **sole** serialization guard. `preview_cross_mode_e2e` (issue #1547) runs a real `zfb build` and then spawns both a real `zfb dev` and a real `zfb preview` (static) against that build output, proving the `_redirects`/plugin-middleware behavior landed by epic #1541 is identical across both. `dev_out_of_root_collection_e2e` (issue #1552, epic #1548) covers a configured collection outside the project directory (`allowOutsideRoot: true`) in a live edit→serve loop. `dev_content_aggregate_cold_boot_e2e` (issue #1598) starts a fresh content fixture and proves an entry edit reaches its entry, index, tag, and paginated aggregate routes. `wasm_ssr_dev_smoke_e2e` (issue #1511) serves a `prerender=false` route whose 200 response is derived from an imported Wasm module. `page_extension_route_table_build` (issue #1993, Page Extension Contract epic #1990 Wave 3) is a build-only member: it drives a real `zfb build` proving `.ts`/`.js`/`.jsx` pages reach the production route table and render end-to-end. `page_extension_full_matrix_e2e` (issue #1994, epic #1990 Wave 4 — the central confirm pass) is the newest flock-adopting member: it runs a real `zfb build` THEN spawns a real `zfb dev` session over a single fixture covering all seven routable page-extension shapes (`.tsx`/`.ts`/`.jsx`/`.js`/`.mdx`/`.md`/`.html`) plus a dynamic `.ts` page resolving `paths()`, proving both the production route table and the dev server agree, that no "unrecognised extension" warning fires for any shape in either mode, and (via two fast, non-`#[ignore]`d sibling tests in the same file) that every production page-extension gate — `zfb-router/src/scan.rs`, `zfb-build/src/bundler.rs`, `zfb/src/render_pipeline.rs`, `zfb/src/commands/build.rs` — consumes the shared `zfb_types::ROUTABLE_PAGE_EXTENSIONS` / `SCRIPT_PAGE_EXTENSIONS` constant in code (comments stripped first, so doc-comment prose cannot satisfy the check) and carries no hand-spelled extension list in any spelling. A legitimate non-page cluster (esbuild resolve order, JS-like module extensions, the CSS source scan) opts out with a `page-extension-drift-guard: allow — <why>` comment on or directly above the line; the second sibling test is the detector's own self-test, so it cannot be silently neutered. `sibling_css_module_command_layer_build` (issue #1698, sibling-mirror epic #1691) is a build-only member: it drives a real `zfb build` proving a workspace-sibling `.module.css` class map is discovered and emitted through the command layer (`discover_css_source_files` / `compute_css_module_class_maps`), not manually supplied — Unix-only (`#![cfg(unix)]`), matching `css_modules_components_build`'s own gate. `gfm_footnotes_tasklists_confirm_build` (issue #2029, GFM Footnotes And Task Lists epic #2021 Wave 6 — the central confirm pass) is the newest build-only member: a real `zfb build` over a content collection proving GFM task-list checkboxes and footnotes (including a repeated reference) render byte-identically between `.md` and `.mdx` files, plus a `.mdx`-only fixture proving a footnote/task-list nested inside a custom JSX component's children resolves through the same document-level footnote model, with the footnote section still rendering once at the end of the whole document rather than trapped inside the JSX subtree. (`check_command` and `framework_packages_no_pnpm` are deliberately excluded — the former runs only `zfb check`, the latter calls `zfb_build::bundle` in-process without spawning `zfb` or booting V8.) The group also carries a long per-test `slow-timeout` (`terminate-after` = 600s) so legitimate dev-server watchdogs are never killed as "hung". The same override also carries **3 test-name predicates**, for heavy tests that live inside a shared test target and so cannot be reached by `binary(=...)`: `render_pipeline::tests::eval_deferred_paths_via_worker_embedded_v8_non_literal_paths` and `version_stamp_from_env` by exact name, plus `test(/^v8_host_adapter::tests::/)` — the one PREFIX-regex entry, added in epic #2008's review pass. That module (`crates/zfb/src/v8_host_adapter.rs`) gained ~14 tests in sub-issue #2009 that each boot a REAL V8 isolate on a dedicated thread inside the `zfb` lib-test target, one of which (`drop_closes_channel_and_joins_thread_promptly_on_both_constructors`) asserts a **5s wall-clock** bound on `Drop`'s thread join — serialized rather than loosened, since the bound is the only thing that test pins. The prefix (not 15 exact names) is deliberate: every test in that module is V8-heavy by construction, so a future addition is covered instead of silently unserialized. Verify membership with `cargo nextest show-config test-groups -p zfb --lib` (measured at 15 matched tests, and identical under `--profile ci`). These predicates keep those individual heavy tests serialized against the other 30 binaries, both on the T1 gate and when exam.yml runs the ignored manifest. `embedded_host_request_time_e2e` (issue #2019, V8 Request Time Parity epic #2012 Wave 7 — the confirm pass over Waves 2-6) is the newest flock-adopting member: one `#[tokio::test]` spawns a real `zfb dev` session and drives a `prerender=false` route performing a real outbound loopback fetch AND Web Crypto (`getRandomValues`/`randomUUID`/`subtle.digest`) in one dispatch, plus sibling routes proving explicit resource exhaustion (51 concurrent fetches against the 50-subrequest cap), a host-side transport failure (a released loopback port — ECONNREFUSED), and an unsupported capability (`crypto.subtle.encrypt`) failing with a request-time-specific diagnostic rather than the build-time-only "fetch() called from SSG runtime" wording; a second `#[tokio::test]` in the same binary spawns a real `zfb build` over a separate, SSR-route-free fixture proving the build-time SSG network denial (guardrail 4 of epic #2012) is unchanged. Like `wasm_ssr_dev_smoke_e2e`, it is NOT `#[ignore]`d — it self-skips at runtime via `locate_esbuild()` and the `embed_v8`/"no esbuild" exit-log convention.
  - **required-features lane.** `cargo nextest run -p zfb-md-extras --features test-utils` DOES pick up the `required-features = ["test-utils"]`-gated targets (verified in #1340), so that lane runs under nextest too.
  - **Inventory parity is guaranteed by reconciliation, not assumed.** #1340 diffed `cargo test --workspace -- --list` against `cargo nextest list --workspace --run-ignored all`: byte-identical (3295 = 3295) apart from doctests. No test target uses a custom `harness = false`, so nextest can run every binary. If you add a custom-harness or benchmark test target, re-run that reconciliation — nextest silently skips binaries it cannot drive.

### `#[ignore]` manifest (Rust)

Every `#[ignore]`d Rust test carries a reason starting with one of 5 machine-greppable taxonomy prefixes — `grep -rn '#\[ignore = "' crates/` and spot-check the prefix:

- `env-gate: <binary> — <how to run>` — needs an external binary present (no issue URL needed).
- `heavy: run with --ignored — <why>` — deliberate heavy/local-only test (no issue URL needed).
- `flaky: <issue-url>` — quarantined flaky test (issue URL mandatory).
- `verification: <why>` — one-time "it was done" proof, not a regression guard (no issue URL needed).
- `pending-feature: <issue-url>` — blocked on an unimplemented product feature or unwritten test body (issue URL mandatory).

Parity between this table's `env-gate:`/`heavy:`-tagged rows and their scheduled home (an exam.yml filterset entry or a health.yml `-- --ignored` step scope) is no longer a point-in-time manual audit — `scripts/check-exam-ignore-parity.sh` (issue #2072) recomputes the diff mechanically by parsing both files' actual scopes, with an offline fixture-based unit test at `tests/unit/check-exam-ignore-parity.sh`; run either before trusting this table's coverage claims.

Audited and retagged in full during issue #1337 (2026-07); reconciled again during issue #1504 (2026-07); a third cross-pipeline env-gate joined during issue #1674 (2026-07); a dev workspace-sibling watch e2e joined + the pre-existing `shared_bundle_discovers_marked_glue_and_wasm_resources` env-gate row (missing since #1674) restored during issue #1678 (2026-07); a `pending-feature` row joined during issue #1698 (2026-07, sibling-mirror epic confirm pass — issue #1724); a stage-escape-guards confirm env-gate joined during issue #1708 (2026-07); a Tailwind `@source` sibling-mirror-root env-gate joined during issue #1776 (2026-07); a stage-escape-audit ACCEPTANCE env-gate (beside the rejection test #1708 added) joined during issue #1796 (2026-07, Stage Audit Fix epic #1794 Wave 2) — the same edit also corrected pre-existing stale line numbers for every other `crates/zfb-islands/tests/integration.rs` row below, drifted since #1708 added the rejection test ahead of them; a skip-dir-exclusion leak-proof env-gate joined during issue #1803 (2026-07, Tailwind Sibling Source epic #1799 Wave 2, gap b) — the same edit also corrected the neighbouring `sibling_css_module_command_layer_build.rs` row's stale line number, drifted since #1776; a dev-e2e Tailwind-sibling env-gate joined during issue #1805 (2026-07, Tailwind Sibling Source epic #1799 Wave 3, gap (a) confirm pass); a first-party dot-path staging real-esbuild confirm env-gate joined during issue #1841 (2026-07, epic #1836, confirming #1840); three workspace-root alias real-esbuild regressions joined during issue #1885 (2026-07, epic #1883, confirming #1884); an in-project entry temp-file hygiene env-gate joined during issue #1976 (2026-07, Sweep Cleanups epic #1975, fixing #1970); three more `pending-feature` rows joined during issue #1983 (2026-07, Staging Correctness epic #1982 Wave 1) extending the #1724 red-test matrix to a wildcard-alias `import.meta.glob` cell plus both alias shapes of the module-worker `new URL(...)` macro — the matrix's other candidate cell, `?raw` under both alias shapes, was PROVEN not to reproduce (a separate preflight pass in `mirror_sibling_root` already covers it) and landed as two ordinary passing regression tests instead, not `#[ignore]`d rows; two consume-from-source RED regressions joined during issue #2039 (2026-07, Staging Correctness epic #1982 Wave 1); two root-workspace stage-escape-audit-disabled RED regressions (one SSR, one islands/client) joined during issue #1984 (2026-07, Staging Correctness epic #1982 Wave 1); all **4** `pending-feature: #1724` rows were DELETED during issue #1985 (2026-07, Staging Correctness epic #1982 Wave 2) — the fix landed, the four tests assert unignored, and the `//`-comment occurrence of the literal attribute text that used to inflate a naive grep by 1 went with them — a dist-shipping-sibling negative joined and the #2039 nested-member row was renamed (its RED assertion inverted to green) during issue #2040 (2026-07, Staging Correctness epic #1982 Wave 4 — the case-2 redefinition); the islands/client half of #1984's RED row was renamed (its RED assertion inverted to a stage-escape REJECTION) during issue #1987 (2026-07, Staging Correctness epic #1982 Wave 5 — wiring the islands/client audit site); the SSR half of #1984's row was renamed the same way (`bundler_root_workspace_stage_escape_audit_disabled_regression.rs` → `bundler_root_workspace_stage_escape_audit_armed_regression.rs`) during issue #1988 (2026-07, Staging Correctness epic #1982 Wave 6 — wiring the SSR audit site, the epic's P1 silent leg); three full-CLI cross-pipeline confirm rows joined during issue #1989 (2026-07, Staging Correctness epic #1982 Wave 7 — the central confirm pass), which is the authoritative final reconciliation of this table for the epic: every stale `file:line` below was re-measured against the actual `fn`/`async fn` declaration line (the table's now-consistent convention — several rows, especially in `crates/zfb-islands/tests/integration.rs` and `crates/zfb/src/commands/build.rs`, had drifted by anywhere from 1 to over 2000 lines since #1340/#1504; the `bundler_consume_from_source_esbuild_regression.rs` row also had its 2nd/3rd `:line`/name pairing swapped, fixed here too). An epic-level review pass on epic #1982 then WIRED the two previously-unwired `zfb-build` binaries into `health.yml` ("Run zfb-build consume-from-source esbuild regression test", "Run zfb-build root-workspace stage-escape audit armed regression test") and corrected all three rows whose "Where / how it runs" text promised CI wiring that did not exist — the `commands::build.rs` islands row was in fact ALREADY covered by the existing `commands::build:: -- --ignored` step, and only its text was wrong. Neither new binary joins `scripts/run-b4push.sh`: the two sibling `zfb-build` esbuild regression binaries (`bundler_dot_path_staging_esbuild_regression`, `bundler_workspace_root_alias_esbuild_regression`) are health.yml-only too, so the B4PUSH_FULL env-gate set stays at its documented 5 steps. Three more `pending-feature` rows joined during issue #1991 (2026-07, Page Extension Contract epic #1990 Wave 1, characterization pass): `.ts`/`.js`/`.jsx` page sources are bundle-capable (per the bundler's own page-entry docs) but the router's `ACCEPTED_PAGE_EXTENSIONS` excludes them, so a characterization test asserting each is ROUTED was written RED-and-ignored, alongside a green sibling pinning the CURRENT skip+warn behavior for the same extension; all **3** of those `pending-feature: #1990` rows were DELETED during issue #1992 (2026-07, Page Extension Contract epic #1990 Wave 2): `zfb-types` now publishes `ROUTABLE_PAGE_EXTENSIONS`/`SCRIPT_PAGE_EXTENSIONS` (`crates/zfb-types/src/page_extensions.rs`) as the single source of truth, `zfb-router`'s `ACCEPTED_PAGE_EXTENSIONS` and `zfb-build`'s `derive_route` both consume it instead of carrying their own literal, the router widened to accept `.ts`/`.js`/`.jsx`, the three RED tests flipped green with their `#[ignore]` dropped, and their now-stale green skip+warn siblings were deleted. A dev-loop RED repro env-gate joined during issue #1996 (2026-07, Mirror Root CSS Scan epic #1995 Wave 1): a sibling mirror root claimed ONLY through a tsconfig alias (no import touches it, so the file-parent `watch_additional_files` channel cannot keep it watched and mask the result) whose `.mdx` carries a utility class found nowhere else — the watch event is PROVEN to arrive (`tick(): kinds=[notes.mdx:Modified]`) while the Tailwind scan never reruns, because an out-of-root `.md`/`.mdx` classifies as `PathClass::Content` and the #1288 `mark_css` rule is gated on `Module` only. Wave 2 (#1997) inverts its assertion. A `heavy` RED e2e joined during issue #2001 (2026-07, Dev Self Heal epic #1999 Wave 1): at a fresh Cold (`ZFB_DEV_BOOT_LAZY=cold`) boot, a `_redirects` `200` rewrite (`/alias -> /target`) 404s indefinitely because `serve_from_waterfall` (routes.rs) deliberately bypasses the render-on-request hook for a rewrite target — the test polls `/alias` repeatedly, interleaving each poll with a genuine SSE-confirmed unrelated watcher tick (an edit to the unrelated `pages/index.tsx`, never `/target` itself), and fails today because `/alias` never resolves. Waves 3-4 of the same epic (#2003, #2004) landed the fix — #2003's pure enumerator (`zfb_server::prewarm_rewrite_targets`) plus #2004's boot-time pre-warm pass (`crates/zfb/src/dev_rewrite_prewarm.rs`), which marks each enumerated target stale and renders it through the adapter's own claim -> render -> guarded-write core, entirely OUTSIDE any request path, so `serve_from_waterfall` still re-runs nothing for a rewrite. The pass has THREE registration points, all outside any request: the deferred boot task, the `_redirects` watch task (so a live edit RE-REGISTERS the target set — no restart), and a one-shot latch raised by `recover_cold_bootstrap_after_publish` and drained in `on_outcome` (a #1809 recovery republishes tables the boot pre-warm ran against while still empty; `mark_all_routes_stale` alone leaves the target claimable but nothing ever claims it). The RED test was inverted and renamed accordingly (`cold_rewrite_red_e2e.rs` -> `cold_rewrite_prewarm_e2e.rs`) and stays `heavy` — it is no longer RED, but a real dev-server E2E is still too costly for the T1 gate; all **3** of those `pending-feature: #1990` rows were DELETED during issue #1992 (2026-07, Page Extension Contract epic #1990 Wave 2): `zfb-types` now publishes `ROUTABLE_PAGE_EXTENSIONS`/`SCRIPT_PAGE_EXTENSIONS` (`crates/zfb-types/src/page_extensions.rs`) as the single source of truth, `zfb-router`'s `ACCEPTED_PAGE_EXTENSIONS` and `zfb-build`'s `derive_route` both consume it instead of carrying their own literal, the router widened to accept `.ts`/`.js`/`.jsx`, the three RED tests flipped green with their `#[ignore]` dropped, and their now-stale green skip+warn siblings were deleted. A dev-loop RED repro env-gate joined during issue #1996 (2026-07, Mirror Root CSS Scan epic #1995 Wave 1): a sibling mirror root claimed ONLY through a tsconfig alias (no import touches it, so the file-parent `watch_additional_files` channel cannot keep it watched and mask the result) whose `.mdx` carries a utility class found nowhere else — the watch event is PROVEN to arrive (`tick(): kinds=[notes.mdx:Modified]`) while the Tailwind scan never reruns, because an out-of-root `.md`/`.mdx` classifies as `PathClass::Content` and the #1288 `mark_css` rule is gated on `Module` only. Wave 2 (#1997) inverts its assertion. Two more `env-gate` RED rows joined during issue #2000 (2026-07, Dev Self Heal epic #1999 Wave 1): `outcome_to_events` (`crates/zfb-server/src/livereload.rs`) gates `ReloadEvent::Page` on `pages_written`/`pages_stale`/`pages_pruned` non-empty or `client_scripts_changed`, and both self-heal channels for the deferred window (`mark_all_routes_stale`, called by the healthy Cold-lazy deferred publish per #1182/#1808, AND by `recover_cold_bootstrap_after_publish`'s cold-bootstrap recovery per #1809) walk ONLY `routes_by_source` (the SSG table) — `prerender = false` routes live in the separate `ssr_routes` table and are never marked, so an SSR-only project's `pages_stale` drains empty on EITHER path and no reload reaches an already-open tab, even though the SSR route already serves 200 per-request. Wave 2 (#2002) fixed it with ONE shared `BuildOutcome::ssr_routes_published` bit — raised by both channels through the single `DevRenderSession::note_ssr_routes_published` seam, drained per-tick by the boot hook's fold and by `DevAssetPipeline`'s new `SsrPublishProbe`, and folded into `outcome_to_events`'s EXISTING single `Page` gate (so a mixed SSG/SSR tick setting both `pages_stale` and the new bit still emits exactly one event, and an SSG-only project never raises the bit at all). Both RED e2e tests were inverted and renamed accordingly, and Wave 1's companion unit pin was inverted in place (`ssr_only_deferred_publish_outcome_emits_no_page_event` → `ssr_only_deferred_publish_outcome_emits_single_page_event`, still NOT ignored) beside `outcome_to_events`'s existing test suite. Deliberately NO server-side staleness machinery was added for SSR routes — they need none; this is a signalling fix only. A `wrangler dev` (real workerd) env-gate joined during issue #2020 (2026-07, V8 Request Time Parity epic #2012 Wave 8 — the epic's central confirm pass): the same three request-time SSR page sources (`fetch` + Web Crypto happy path; a legacy-MD5 `crypto.subtle.digest` call; a full AES-GCM generateKey→encrypt→decrypt round trip) are built once for `@takazudo/zfb-adapter-cloudflare` and served unmodified a second time under `zfb dev`, then diffed against a real, workspace-pinned (4.85.0) `wrangler dev` local-mode process — proving supported contract rows are byte-identical across runtimes (including the SHA-256 digest of a fixed input) and that the embedded host's D7 (no MD5) and D8 (key-bearing SubtleCrypto fails closed) divergences are real: production genuinely succeeds at both where the embedded host genuinely fails closed with the contract's exact `NotSupportedError` diagnostics. Six `pending-feature` rows joined during issue #2022 (2026-07, GFM Footnotes And Task Lists epic #2021 Wave 1): RED characterization tests for GFM task-list checkbox emission (superseding #1950) — `ListItem.checked` is parsed by the `markdown` crate's GFM task-list tokenizer but read by none of the three `ListItem` emit sites. Four rows live in `crates/zfb-content/tests/gfm_task_list_red.rs`, covering Site 1 (`pipeline::mdast_to_hast_inner`'s `ListItem` arm, exercised via `render_html`) and Site 3 (`mdx_jsx_emit::jsx_render_child`'s `ListItem` arm, exercised via `render_mdx_jsx_module` with the task list nested inside an MDX JSX element body — the only shape that reaches that call site). Two more rows live directly inside `crates/zfb-content/src/mdx_jsx_emit.rs`'s own `#[cfg(test)] mod tests`, unit-testing Site 2 (`JsxEmitter::emit_node`'s `ListItem` arm) by constructing the mdast node by hand — that site is structurally UNREACHABLE through any public entry point with `checked: Some(_)` today, because the only call path that reaches `JsxEmitter::emit_node` (no pipeline supplied) always resolves GFM constructs to `ResolvedGfmConstructs::CONSERVATIVE` (`task_list_item: false` hard-coded), and supplying a pipeline to turn the construct on forces a hast detour that routes a top-level list through Site 1 instead. All six assert the fix's desired contract: a checkbox `type="checkbox"` input, `checked` present/absent per the item's state, `disabled` (static server-rendered output), with item text preserved — verified RED for the right reason (today, no checkbox is emitted at all; the marker text is simply gone). Three companion control tests (not ignored) pin that the flag-off path (`taskListItem: false`) keeps the literal `- [ ]` marker text visible and never emits a checkbox, and that a plain non-task-list item never grows a checkbox. Ten `pending-feature` rows joined during issue #2023 (2026-07, GFM Footnotes And Task Lists epic #2021, Wave 1 — RED characterization tests for the footnote half of superseded issue #1950): `FootnoteDefinition`/`FootnoteReference` mdast nodes fall into catch-alls in both `crates/zfb-content/src/pipeline.rs` (`mdast_to_hast_inner`, shared by `Pipeline::run` and the JSX-emit hast detour) and `crates/zfb-content/src/mdx_jsx_emit.rs` (`jsx_render_child`, fired only for footnotes nested inside an MDX JSX element body) with no diagnostic — both the reference marker and the definition body vanish silently. Four rows in each file pin DOCUMENT-LEVEL behaviour (reference/definition association, per-occurrence distinct backreference ids for repeated references, reference-order numbering/ordering across multiple definitions, and duplicate-definition collapse-to-one) via structural tree/string walks that deliberately avoid hardcoding the exact id/href STRING scheme — that escaping/allocation policy belongs to the document-level model sub-issue #2025. Two more rows in `mdx_jsx_emit.rs` cover the nested-JSX shapes #2027 (JSX footnote emission) calls out by name: a reference+definition nested inside an MDX JSX element body, and a definition whose own body contains JSX. A fifth case per file — a reference with no matching definition — is a **passing, non-ignored** characterization pin instead of a RED test: `markdown-rs`'s footnote constructs only recognise `[^label]` as a reference when a matching `[^label]: …` definition exists elsewhere in the document, so an unmatched reference never becomes a `FootnoteReference` node at all and stays literal text today, regardless of the catch-all bug — confirmed by inspecting the parsed mdast tree directly via a throwaway `zfb-content` example during the investigation (not committed). All **6** `pending-feature: #2024` rows were DELETED during issue #2024 (2026-07, GFM Footnotes And Task Lists epic #2021 Wave 2): the fix landed at all three `ListItem` emit sites (`pipeline.rs`'s `mdast_to_hast_inner`, and both `mdx_jsx_emit.rs` sites — `JsxEmitter::emit_node` and `jsx_render_child`), Option B (minimal compatible checkbox rendering, per #2024's own default-if-undecided clause — see #2028 for the documented scope) — a disabled `<input type="checkbox">` prefixed before the item's own content, `checked` present only when the item is checked — and all six tests assert unignored with none of their assertions touched. All **4** `pending-feature: #2026` rows for `crates/zfb-content/src/pipeline.rs` were DELETED during issue #2026 (2026-07, GFM Footnotes And Task Lists epic #2021 Wave 3 — HAST/HTML footnote emission): `mdast_to_hast_with` now builds a `FootnoteModel` (#2025) once per document and threads it through the existing `mdast_to_hast_inner` recursive walk via a `FootnoteRenderCtx` (a `RefCell<FootnoteCursor>`, since claiming "the next occurrence of this identifier" needs `&mut` in an otherwise shared-reference walk) — a `FootnoteReference` renders as a `<sup><a href="#fn-id" id="fnref-id" data-footnote-ref …>N</a></sup>` marker claimed in document order, a `FootnoteDefinition` renders as nothing in place (matching every other footnote body — they only render once, collected), and the model's entries render as a `<section data-footnotes><h2 class="sr-only" id="footnote-label">…</h2><ol>…</ol></section>` appended as the Root's last child, each `<li>` carrying one backreference `<a data-footnote-backref>` per occurrence. All four tests assert unignored with no assertion touched. Two new non-ignored tests joined `pipeline.rs` in the same commit: one proves `gfm: false` still renders footnote syntax as literal text with no section appended (structurally guaranteed here, same as the task-list wave's gating, since `footnote_definition: false` means markdown-rs never produces a `FootnoteReference`/`FootnoteDefinition` node in the first place); the other proves the catch-all still swallows a reference-style link definition (`MdastNode::Definition`) now that footnotes are carved out of it. The four `crates/zfb-content/src/mdx_jsx_emit.rs` `jsx_*` tests driven by this SAME shared catch-all were confirmed to pass mechanically once unignored, but were deliberately left `#[ignore]`d — #2026's assignment scoped it to the HTML/HAST path, and un-ignoring them was left to wave 4 (#2027) alongside the two nested-JSX rows so that wave's own confirm pass covers them. All **6** `pending-feature: #2026`/`#2027` rows for `crates/zfb-content/src/mdx_jsx_emit.rs` were DELETED during issue #2027 (2026-07, GFM Footnotes And Task Lists epic #2021 Wave 4 — JSX-emit footnote emission, the epic's confirm pass over the JSX path): the four `#2026`-tagged rows turned out to already pass mechanically (as noted above) and were simply un-`#[ignore]`d with no assertion touched. The two `#2027`-tagged nested-JSX rows needed a real fix — `jsx_render_child`'s own catch-all (the shape a footnote reference/definition nested inside an MDX JSX element body reaches, a SEPARATE recursive descent from `pipeline::mdast_to_hast_inner`'s) had no access to the `FootnoteModel`/cursor `mdast_to_hast_with` builds once per document. Rather than building a second, independently-advancing model in this file (which risks drifting out of sync with the main walk's occurrence numbering for a document mixing top-level and JSX-nested references to one identifier), `JsxEmitStrategy::JsxPath`'s closure signature was widened to `Fn(&MdastNode, &FootnoteRenderCtx<'_>) -> String` — `FootnoteRenderCtx` (`pipeline.rs`) gained a `pub fn next_reference` and was made `pub` (not `pub(crate)`: it sits in a variant of the already-`pub` `JsxEmitStrategy` enum, so a lower-visibility type there is a "private type in public interface" error) — and `emit_jsx_raw`/its one call site now thread the SAME `fc` the main walk is using down into the closure. `mdx_jsx_emit.rs` threads that `fc` as an explicit parameter through `jsx_raw_recursive` → `jsx_element_text` → `jsx_render_child` → `jsx_render_table`/`jsx_wrap_children`; `jsx_render_child`'s `FootnoteDefinition` arm renders nothing (matching the shared catch-all's arm — the body only ever renders once, in the collected footer section) and its `FootnoteReference` arm claims the next occurrence via `fc.next_reference` and renders `jsx_footnote_reference_marker`, a JSX-shaped counterpart to `pipeline::footnote_reference_marker` routed through `_components.sup`/`_components.a` and rendering the number via `js_string_literal_in_braces` so it produces the identical `{"N"}` shape `HastJsxBridge`'s `HastNode::Text` arm already produces for the top-level path. Two new non-ignored tests joined `mdx_jsx_emit.rs` in the same commit, mirroring `pipeline.rs`'s own pair: `jsx_catch_all_still_swallows_reference_style_link_definitions_nested_in_jsx` proves `jsx_render_child`'s catch-all still drops a reference-style link definition nested inside an MDX JSX element body now that footnotes are carved out of it, and `jsx_gfm_false_leaves_nested_footnote_syntax_as_literal_text` proves `footnote_definition: false` leaves footnote syntax nested inside an MDX JSX element body as literal text with no footnote-shaped markup anywhere (structurally guaranteed, same as the task-list wave's and #2026's own gating, since markdown-rs never produces a `FootnoteReference`/`FootnoteDefinition` node at all with the construct off). A `pending-feature` row joined during issue #2031 (2026-07, Mermaid Minify Preservation epic #2030 Wave 1): a RED test proving `minify_rendered_html_bytes`/`minify_rendered_html_string` (`crates/zfb/src/commands/html_minify.rs`) collapses whitespace inside a `<div class="mermaid" data-mermaid>` body — the exact shape `zfb-md-extras::mermaid`'s `MermaidPlugin` emits by rewriting a `<pre><code class="language-mermaid">` block. The fixture deliberately includes `subgraph build["zfb build — your machine"]` (an id flush against `[` with no space), the shape zudo-doc's client-side `normalizeCollapsedMermaidSource` repair heuristic cannot fix; a weaker fixture using only plain `A --> B` node declarations would still pass under a fix that merely reinserts newlines between top-level statements. The assertion is written in desired post-fix form (`assert_eq!(output, input)`, byte-identical) so Wave 3 (#2033, which wires the extraction/restoration helper Wave 2 (#2032) builds into the minifier) can flip it green without touching the assertion. The `pending-feature: #2033` row was DELETED during issue #2033 (2026-07, Mermaid Minify Preservation epic #2030 Wave 3): `minify_rendered_html_bytes` (`crates/zfb/src/commands/html_minify.rs`) now calls `mermaid_preserve::extract` before minifying and `MermaidPreservation::restore` after, falling back to the original bytes on any `MermaidRestoreError` per the helper's own documented policy; the RED test asserts unignored with its assertion untouched. Two more `pending-feature` rows joined during issue #2080 (2026-07, Staging Correctness 2 epic #2078 Wave 1): RED/pin tests for issue #2049's non-monotonic mirror-derived materialisation — `materialise_source_file` (`crates/zfb-build/src/bundler.rs`) mutates `glob_matched_files`, `raw_import_edges`, and `module_worker_dependencies`, and writes generated `.zfb-raw-*.mjs` modules to disk, all BEFORE its final `write_if_changed(to, ...)` commit; a late failure at that final step (forced in the test by pre-creating `to` as a directory) today leaves every one of those mutations in place. One test pins the orphan disk file plus all three leaked accumulators; the other pins the sharpest consequence — the leaked `raw_import_edges` entry flips `raw_target_matches` for a completely unrelated sibling file (`victim.ts`), so its own `import.meta.glob` never gets expanded and it materialises as a raw copy instead of going through its normal transform path. Both assertions are written in desired post-fix form (empty accumulators / no residue) and verified RED today for exactly those reasons. A non-ignored control test (`materialise_source_file_successful_call_commits_all_four_effects`) pins that a SUCCESSFUL call still commits all four effects — the case the eventual fix (issue #2085, provenance threading) must not break. Per epic #2078's corrected flip protocol, both `pending-feature` tests are unit/self-skipping (no external binary dependency, no esbuild/tailwind needed), so the later flipping sub (#2085) must DELETE the `#[ignore]` attribute entirely rather than replace it with an `env-gate:` tag.
Audited and retagged in full during issue #1337 (2026-07); reconciled again during issue #1504 (2026-07); a third cross-pipeline env-gate joined during issue #1674 (2026-07); a dev workspace-sibling watch e2e joined + the pre-existing `shared_bundle_discovers_marked_glue_and_wasm_resources` env-gate row (missing since #1674) restored during issue #1678 (2026-07); a `pending-feature` row joined during issue #1698 (2026-07, sibling-mirror epic confirm pass — issue #1724); a stage-escape-guards confirm env-gate joined during issue #1708 (2026-07); a Tailwind `@source` sibling-mirror-root env-gate joined during issue #1776 (2026-07); a stage-escape-audit ACCEPTANCE env-gate (beside the rejection test #1708 added) joined during issue #1796 (2026-07, Stage Audit Fix epic #1794 Wave 2) — the same edit also corrected pre-existing stale line numbers for every other `crates/zfb-islands/tests/integration.rs` row below, drifted since #1708 added the rejection test ahead of them; a skip-dir-exclusion leak-proof env-gate joined during issue #1803 (2026-07, Tailwind Sibling Source epic #1799 Wave 2, gap b) — the same edit also corrected the neighbouring `sibling_css_module_command_layer_build.rs` row's stale line number, drifted since #1776; a dev-e2e Tailwind-sibling env-gate joined during issue #1805 (2026-07, Tailwind Sibling Source epic #1799 Wave 3, gap (a) confirm pass); a first-party dot-path staging real-esbuild confirm env-gate joined during issue #1841 (2026-07, epic #1836, confirming #1840); three workspace-root alias real-esbuild regressions joined during issue #1885 (2026-07, epic #1883, confirming #1884); an in-project entry temp-file hygiene env-gate joined during issue #1976 (2026-07, Sweep Cleanups epic #1975, fixing #1970); three more `pending-feature` rows joined during issue #1983 (2026-07, Staging Correctness epic #1982 Wave 1) extending the #1724 red-test matrix to a wildcard-alias `import.meta.glob` cell plus both alias shapes of the module-worker `new URL(...)` macro — the matrix's other candidate cell, `?raw` under both alias shapes, was PROVEN not to reproduce (a separate preflight pass in `mirror_sibling_root` already covers it) and landed as two ordinary passing regression tests instead, not `#[ignore]`d rows; two consume-from-source RED regressions joined during issue #2039 (2026-07, Staging Correctness epic #1982 Wave 1); two root-workspace stage-escape-audit-disabled RED regressions (one SSR, one islands/client) joined during issue #1984 (2026-07, Staging Correctness epic #1982 Wave 1); all **4** `pending-feature: #1724` rows were DELETED during issue #1985 (2026-07, Staging Correctness epic #1982 Wave 2) — the fix landed, the four tests assert unignored, and the `//`-comment occurrence of the literal attribute text that used to inflate a naive grep by 1 went with them — a dist-shipping-sibling negative joined and the #2039 nested-member row was renamed (its RED assertion inverted to green) during issue #2040 (2026-07, Staging Correctness epic #1982 Wave 4 — the case-2 redefinition); the islands/client half of #1984's RED row was renamed (its RED assertion inverted to a stage-escape REJECTION) during issue #1987 (2026-07, Staging Correctness epic #1982 Wave 5 — wiring the islands/client audit site); the SSR half of #1984's row was renamed the same way (`bundler_root_workspace_stage_escape_audit_disabled_regression.rs` → `bundler_root_workspace_stage_escape_audit_armed_regression.rs`) during issue #1988 (2026-07, Staging Correctness epic #1982 Wave 6 — wiring the SSR audit site, the epic's P1 silent leg); three full-CLI cross-pipeline confirm rows joined during issue #1989 (2026-07, Staging Correctness epic #1982 Wave 7 — the central confirm pass), which is the authoritative final reconciliation of this table for the epic: every stale `file:line` below was re-measured against the actual `fn`/`async fn` declaration line (the table's now-consistent convention — several rows, especially in `crates/zfb-islands/tests/integration.rs` and `crates/zfb/src/commands/build.rs`, had drifted by anywhere from 1 to over 2000 lines since #1340/#1504; the `bundler_consume_from_source_esbuild_regression.rs` row also had its 2nd/3rd `:line`/name pairing swapped, fixed here too). An epic-level review pass on epic #1982 then WIRED the two previously-unwired `zfb-build` binaries into `health.yml` ("Run zfb-build consume-from-source esbuild regression test", "Run zfb-build root-workspace stage-escape audit armed regression test") and corrected all three rows whose "Where / how it runs" text promised CI wiring that did not exist — the `commands::build.rs` islands row was in fact ALREADY covered by the existing `commands::build:: -- --ignored` step, and only its text was wrong. Neither new binary joins `scripts/run-b4push.sh`: the two sibling `zfb-build` esbuild regression binaries (`bundler_dot_path_staging_esbuild_regression`, `bundler_workspace_root_alias_esbuild_regression`) are health.yml-only too, so the B4PUSH_FULL env-gate set stays at its documented 5 steps. Three more `pending-feature` rows joined during issue #1991 (2026-07, Page Extension Contract epic #1990 Wave 1, characterization pass): `.ts`/`.js`/`.jsx` page sources are bundle-capable (per the bundler's own page-entry docs) but the router's `ACCEPTED_PAGE_EXTENSIONS` excludes them, so a characterization test asserting each is ROUTED was written RED-and-ignored, alongside a green sibling pinning the CURRENT skip+warn behavior for the same extension; all **3** of those `pending-feature: #1990` rows were DELETED during issue #1992 (2026-07, Page Extension Contract epic #1990 Wave 2): `zfb-types` now publishes `ROUTABLE_PAGE_EXTENSIONS`/`SCRIPT_PAGE_EXTENSIONS` (`crates/zfb-types/src/page_extensions.rs`) as the single source of truth, `zfb-router`'s `ACCEPTED_PAGE_EXTENSIONS` and `zfb-build`'s `derive_route` both consume it instead of carrying their own literal, the router widened to accept `.ts`/`.js`/`.jsx`, the three RED tests flipped green with their `#[ignore]` dropped, and their now-stale green skip+warn siblings were deleted. A dev-loop RED repro env-gate joined during issue #1996 (2026-07, Mirror Root CSS Scan epic #1995 Wave 1): a sibling mirror root claimed ONLY through a tsconfig alias (no import touches it, so the file-parent `watch_additional_files` channel cannot keep it watched and mask the result) whose `.mdx` carries a utility class found nowhere else — the watch event is PROVEN to arrive (`tick(): kinds=[notes.mdx:Modified]`) while the Tailwind scan never reruns, because an out-of-root `.md`/`.mdx` classifies as `PathClass::Content` and the #1288 `mark_css` rule is gated on `Module` only. Wave 2 (#1997) inverts its assertion. A `heavy` RED e2e joined during issue #2001 (2026-07, Dev Self Heal epic #1999 Wave 1): at a fresh Cold (`ZFB_DEV_BOOT_LAZY=cold`) boot, a `_redirects` `200` rewrite (`/alias -> /target`) 404s indefinitely because `serve_from_waterfall` (routes.rs) deliberately bypasses the render-on-request hook for a rewrite target — the test polls `/alias` repeatedly, interleaving each poll with a genuine SSE-confirmed unrelated watcher tick (an edit to the unrelated `pages/index.tsx`, never `/target` itself), and fails today because `/alias` never resolves. Waves 3-4 of the same epic (#2003, #2004) landed the fix — #2003's pure enumerator (`zfb_server::prewarm_rewrite_targets`) plus #2004's boot-time pre-warm pass (`crates/zfb/src/dev_rewrite_prewarm.rs`), which marks each enumerated target stale and renders it through the adapter's own claim -> render -> guarded-write core, entirely OUTSIDE any request path, so `serve_from_waterfall` still re-runs nothing for a rewrite. The pass has THREE registration points, all outside any request: the deferred boot task, the `_redirects` watch task (so a live edit RE-REGISTERS the target set — no restart), and a one-shot latch raised by `recover_cold_bootstrap_after_publish` and drained in `on_outcome` (a #1809 recovery republishes tables the boot pre-warm ran against while still empty; `mark_all_routes_stale` alone leaves the target claimable but nothing ever claims it). The RED test was inverted and renamed accordingly (`cold_rewrite_red_e2e.rs` -> `cold_rewrite_prewarm_e2e.rs`) and stays `heavy` — it is no longer RED, but a real dev-server E2E is still too costly for the T1 gate; all **3** of those `pending-feature: #1990` rows were DELETED during issue #1992 (2026-07, Page Extension Contract epic #1990 Wave 2): `zfb-types` now publishes `ROUTABLE_PAGE_EXTENSIONS`/`SCRIPT_PAGE_EXTENSIONS` (`crates/zfb-types/src/page_extensions.rs`) as the single source of truth, `zfb-router`'s `ACCEPTED_PAGE_EXTENSIONS` and `zfb-build`'s `derive_route` both consume it instead of carrying their own literal, the router widened to accept `.ts`/`.js`/`.jsx`, the three RED tests flipped green with their `#[ignore]` dropped, and their now-stale green skip+warn siblings were deleted. A dev-loop RED repro env-gate joined during issue #1996 (2026-07, Mirror Root CSS Scan epic #1995 Wave 1): a sibling mirror root claimed ONLY through a tsconfig alias (no import touches it, so the file-parent `watch_additional_files` channel cannot keep it watched and mask the result) whose `.mdx` carries a utility class found nowhere else — the watch event is PROVEN to arrive (`tick(): kinds=[notes.mdx:Modified]`) while the Tailwind scan never reruns, because an out-of-root `.md`/`.mdx` classifies as `PathClass::Content` and the #1288 `mark_css` rule is gated on `Module` only. Wave 2 (#1997) inverts its assertion. Two more `env-gate` RED rows joined during issue #2000 (2026-07, Dev Self Heal epic #1999 Wave 1): `outcome_to_events` (`crates/zfb-server/src/livereload.rs`) gates `ReloadEvent::Page` on `pages_written`/`pages_stale`/`pages_pruned` non-empty or `client_scripts_changed`, and both self-heal channels for the deferred window (`mark_all_routes_stale`, called by the healthy Cold-lazy deferred publish per #1182/#1808, AND by `recover_cold_bootstrap_after_publish`'s cold-bootstrap recovery per #1809) walk ONLY `routes_by_source` (the SSG table) — `prerender = false` routes live in the separate `ssr_routes` table and are never marked, so an SSR-only project's `pages_stale` drains empty on EITHER path and no reload reaches an already-open tab, even though the SSR route already serves 200 per-request. Wave 2 (#2002) fixed it with ONE shared `BuildOutcome::ssr_routes_published` bit — raised by both channels through the single `DevRenderSession::note_ssr_routes_published` seam, drained per-tick by the boot hook's fold and by `DevAssetPipeline`'s new `SsrPublishProbe`, and folded into `outcome_to_events`'s EXISTING single `Page` gate (so a mixed SSG/SSR tick setting both `pages_stale` and the new bit still emits exactly one event, and an SSG-only project never raises the bit at all). Both RED e2e tests were inverted and renamed accordingly, and Wave 1's companion unit pin was inverted in place (`ssr_only_deferred_publish_outcome_emits_no_page_event` → `ssr_only_deferred_publish_outcome_emits_single_page_event`, still NOT ignored) beside `outcome_to_events`'s existing test suite. Deliberately NO server-side staleness machinery was added for SSR routes — they need none; this is a signalling fix only. A `wrangler dev` (real workerd) env-gate joined during issue #2020 (2026-07, V8 Request Time Parity epic #2012 Wave 8 — the epic's central confirm pass): the same three request-time SSR page sources (`fetch` + Web Crypto happy path; a legacy-MD5 `crypto.subtle.digest` call; a full AES-GCM generateKey→encrypt→decrypt round trip) are built once for `@takazudo/zfb-adapter-cloudflare` and served unmodified a second time under `zfb dev`, then diffed against a real, workspace-pinned (4.85.0) `wrangler dev` local-mode process — proving supported contract rows are byte-identical across runtimes (including the SHA-256 digest of a fixed input) and that the embedded host's D7 (no MD5) and D8 (key-bearing SubtleCrypto fails closed) divergences are real: production genuinely succeeds at both where the embedded host genuinely fails closed with the contract's exact `NotSupportedError` diagnostics. Six `pending-feature` rows joined during issue #2022 (2026-07, GFM Footnotes And Task Lists epic #2021 Wave 1): RED characterization tests for GFM task-list checkbox emission (superseding #1950) — `ListItem.checked` is parsed by the `markdown` crate's GFM task-list tokenizer but read by none of the three `ListItem` emit sites. Four rows live in `crates/zfb-content/tests/gfm_task_list_red.rs`, covering Site 1 (`pipeline::mdast_to_hast_inner`'s `ListItem` arm, exercised via `render_html`) and Site 3 (`mdx_jsx_emit::jsx_render_child`'s `ListItem` arm, exercised via `render_mdx_jsx_module` with the task list nested inside an MDX JSX element body — the only shape that reaches that call site). Two more rows live directly inside `crates/zfb-content/src/mdx_jsx_emit.rs`'s own `#[cfg(test)] mod tests`, unit-testing Site 2 (`JsxEmitter::emit_node`'s `ListItem` arm) by constructing the mdast node by hand — that site is structurally UNREACHABLE through any public entry point with `checked: Some(_)` today, because the only call path that reaches `JsxEmitter::emit_node` (no pipeline supplied) always resolves GFM constructs to `ResolvedGfmConstructs::CONSERVATIVE` (`task_list_item: false` hard-coded), and supplying a pipeline to turn the construct on forces a hast detour that routes a top-level list through Site 1 instead. All six assert the fix's desired contract: a checkbox `type="checkbox"` input, `checked` present/absent per the item's state, `disabled` (static server-rendered output), with item text preserved — verified RED for the right reason (today, no checkbox is emitted at all; the marker text is simply gone). Three companion control tests (not ignored) pin that the flag-off path (`taskListItem: false`) keeps the literal `- [ ]` marker text visible and never emits a checkbox, and that a plain non-task-list item never grows a checkbox. Ten `pending-feature` rows joined during issue #2023 (2026-07, GFM Footnotes And Task Lists epic #2021, Wave 1 — RED characterization tests for the footnote half of superseded issue #1950): `FootnoteDefinition`/`FootnoteReference` mdast nodes fall into catch-alls in both `crates/zfb-content/src/pipeline.rs` (`mdast_to_hast_inner`, shared by `Pipeline::run` and the JSX-emit hast detour) and `crates/zfb-content/src/mdx_jsx_emit.rs` (`jsx_render_child`, fired only for footnotes nested inside an MDX JSX element body) with no diagnostic — both the reference marker and the definition body vanish silently. Four rows in each file pin DOCUMENT-LEVEL behaviour (reference/definition association, per-occurrence distinct backreference ids for repeated references, reference-order numbering/ordering across multiple definitions, and duplicate-definition collapse-to-one) via structural tree/string walks that deliberately avoid hardcoding the exact id/href STRING scheme — that escaping/allocation policy belongs to the document-level model sub-issue #2025. Two more rows in `mdx_jsx_emit.rs` cover the nested-JSX shapes #2027 (JSX footnote emission) calls out by name: a reference+definition nested inside an MDX JSX element body, and a definition whose own body contains JSX. A fifth case per file — a reference with no matching definition — is a **passing, non-ignored** characterization pin instead of a RED test: `markdown-rs`'s footnote constructs only recognise `[^label]` as a reference when a matching `[^label]: …` definition exists elsewhere in the document, so an unmatched reference never becomes a `FootnoteReference` node at all and stays literal text today, regardless of the catch-all bug — confirmed by inspecting the parsed mdast tree directly via a throwaway `zfb-content` example during the investigation (not committed). All **6** `pending-feature: #2024` rows were DELETED during issue #2024 (2026-07, GFM Footnotes And Task Lists epic #2021 Wave 2): the fix landed at all three `ListItem` emit sites (`pipeline.rs`'s `mdast_to_hast_inner`, and both `mdx_jsx_emit.rs` sites — `JsxEmitter::emit_node` and `jsx_render_child`), Option B (minimal compatible checkbox rendering, per #2024's own default-if-undecided clause — see #2028 for the documented scope) — a disabled `<input type="checkbox">` prefixed before the item's own content, `checked` present only when the item is checked — and all six tests assert unignored with none of their assertions touched. All **4** `pending-feature: #2026` rows for `crates/zfb-content/src/pipeline.rs` were DELETED during issue #2026 (2026-07, GFM Footnotes And Task Lists epic #2021 Wave 3 — HAST/HTML footnote emission): `mdast_to_hast_with` now builds a `FootnoteModel` (#2025) once per document and threads it through the existing `mdast_to_hast_inner` recursive walk via a `FootnoteRenderCtx` (a `RefCell<FootnoteCursor>`, since claiming "the next occurrence of this identifier" needs `&mut` in an otherwise shared-reference walk) — a `FootnoteReference` renders as a `<sup><a href="#fn-id" id="fnref-id" data-footnote-ref …>N</a></sup>` marker claimed in document order, a `FootnoteDefinition` renders as nothing in place (matching every other footnote body — they only render once, collected), and the model's entries render as a `<section data-footnotes><h2 class="sr-only" id="footnote-label">…</h2><ol>…</ol></section>` appended as the Root's last child, each `<li>` carrying one backreference `<a data-footnote-backref>` per occurrence. All four tests assert unignored with no assertion touched. Two new non-ignored tests joined `pipeline.rs` in the same commit: one proves `gfm: false` still renders footnote syntax as literal text with no section appended (structurally guaranteed here, same as the task-list wave's gating, since `footnote_definition: false` means markdown-rs never produces a `FootnoteReference`/`FootnoteDefinition` node in the first place); the other proves the catch-all still swallows a reference-style link definition (`MdastNode::Definition`) now that footnotes are carved out of it. The four `crates/zfb-content/src/mdx_jsx_emit.rs` `jsx_*` tests driven by this SAME shared catch-all were confirmed to pass mechanically once unignored, but were deliberately left `#[ignore]`d — #2026's assignment scoped it to the HTML/HAST path, and un-ignoring them was left to wave 4 (#2027) alongside the two nested-JSX rows so that wave's own confirm pass covers them. All **6** `pending-feature: #2026`/`#2027` rows for `crates/zfb-content/src/mdx_jsx_emit.rs` were DELETED during issue #2027 (2026-07, GFM Footnotes And Task Lists epic #2021 Wave 4 — JSX-emit footnote emission, the epic's confirm pass over the JSX path): the four `#2026`-tagged rows turned out to already pass mechanically (as noted above) and were simply un-`#[ignore]`d with no assertion touched. The two `#2027`-tagged nested-JSX rows needed a real fix — `jsx_render_child`'s own catch-all (the shape a footnote reference/definition nested inside an MDX JSX element body reaches, a SEPARATE recursive descent from `pipeline::mdast_to_hast_inner`'s) had no access to the `FootnoteModel`/cursor `mdast_to_hast_with` builds once per document. Rather than building a second, independently-advancing model in this file (which risks drifting out of sync with the main walk's occurrence numbering for a document mixing top-level and JSX-nested references to one identifier), `JsxEmitStrategy::JsxPath`'s closure signature was widened to `Fn(&MdastNode, &FootnoteRenderCtx<'_>) -> String` — `FootnoteRenderCtx` (`pipeline.rs`) gained a `pub fn next_reference` and was made `pub` (not `pub(crate)`: it sits in a variant of the already-`pub` `JsxEmitStrategy` enum, so a lower-visibility type there is a "private type in public interface" error) — and `emit_jsx_raw`/its one call site now thread the SAME `fc` the main walk is using down into the closure. `mdx_jsx_emit.rs` threads that `fc` as an explicit parameter through `jsx_raw_recursive` → `jsx_element_text` → `jsx_render_child` → `jsx_render_table`/`jsx_wrap_children`; `jsx_render_child`'s `FootnoteDefinition` arm renders nothing (matching the shared catch-all's arm — the body only ever renders once, in the collected footer section) and its `FootnoteReference` arm claims the next occurrence via `fc.next_reference` and renders `jsx_footnote_reference_marker`, a JSX-shaped counterpart to `pipeline::footnote_reference_marker` routed through `_components.sup`/`_components.a` and rendering the number via `js_string_literal_in_braces` so it produces the identical `{"N"}` shape `HastJsxBridge`'s `HastNode::Text` arm already produces for the top-level path. Two new non-ignored tests joined `mdx_jsx_emit.rs` in the same commit, mirroring `pipeline.rs`'s own pair: `jsx_catch_all_still_swallows_reference_style_link_definitions_nested_in_jsx` proves `jsx_render_child`'s catch-all still drops a reference-style link definition nested inside an MDX JSX element body now that footnotes are carved out of it, and `jsx_gfm_false_leaves_nested_footnote_syntax_as_literal_text` proves `footnote_definition: false` leaves footnote syntax nested inside an MDX JSX element body as literal text with no footnote-shaped markup anywhere (structurally guaranteed, same as the task-list wave's and #2026's own gating, since markdown-rs never produces a `FootnoteReference`/`FootnoteDefinition` node at all with the construct off). A `pending-feature` row joined during issue #2031 (2026-07, Mermaid Minify Preservation epic #2030 Wave 1): a RED test proving `minify_rendered_html_bytes`/`minify_rendered_html_string` (`crates/zfb/src/commands/html_minify.rs`) collapses whitespace inside a `<div class="mermaid" data-mermaid>` body — the exact shape `zfb-md-extras::mermaid`'s `MermaidPlugin` emits by rewriting a `<pre><code class="language-mermaid">` block. The fixture deliberately includes `subgraph build["zfb build — your machine"]` (an id flush against `[` with no space), the shape zudo-doc's client-side `normalizeCollapsedMermaidSource` repair heuristic cannot fix; a weaker fixture using only plain `A --> B` node declarations would still pass under a fix that merely reinserts newlines between top-level statements. The assertion is written in desired post-fix form (`assert_eq!(output, input)`, byte-identical) so Wave 3 (#2033, which wires the extraction/restoration helper Wave 2 (#2032) builds into the minifier) can flip it green without touching the assertion. The `pending-feature: #2033` row was DELETED during issue #2033 (2026-07, Mermaid Minify Preservation epic #2030 Wave 3): `minify_rendered_html_bytes` (`crates/zfb/src/commands/html_minify.rs`) now calls `mermaid_preserve::extract` before minifying and `MermaidPreservation::restore` after, falling back to the original bytes on any `MermaidRestoreError` per the helper's own documented policy; the RED test asserts unignored with its assertion untouched. A `pending-feature` row joined during issue #2083 (2026-07, Staging Correctness 2 epic #2078 Wave 1): a RED command-layer test in `crates/zfb/src/commands/build.rs` proving #2048's islands/client silent shape — an ACCEPTED (#2040 declared-entry) consume-from-source sibling reached by bare package name via a query-free `require(...)` call (invisible to the islands scanner's `collect_import_edges`) never triggers the shadow-staging precondition, so its own `import.meta.glob` ships literally, unexpanded, in a GREEN production islands bundle; verified RED by inspecting the emitted bundle bytes directly (no execution). The assertion is written in desired post-fix (expansion) form per epic #2078's flip protocol, with a documented sanctioned-downgrade clause (to a loud-failure assertion) if Wave 4's sub #2090 invokes the fallback instead of full acceptance⇒enrolment coupling. All **3** `pending-feature: #2085` rows (one in `crates/zfb-build/tests/bundler_sibling_mirror_esbuild_regression.rs`, two in `crates/zfb-build/src/bundler.rs`'s own `mod tests`) were DELETED during issue #2085 (2026-07, Staging Correctness 2 epic #2078 Wave 2 — provenance threading): `materialise_source_file` now BUFFERS all four of its side-effect channels into a per-call `MaterialiseStaging` and commits them only on success (so a pre-commit transformation/parse rejection leaves no orphan `.zfb-raw-*.mjs` module, no extended accumulator, and no flipped `raw_target_matches` outcome for an unrelated file — the guarantee is deliberately scoped to PRE-COMMIT rejection and explicitly does NOT claim atomicity across the sequential `write_if_changed` commit itself), and `MaterialiseCtx::glob_matched_files` became a `GlobMatchQueue` tagging every queued match with a `MaterialiseProvenance` (`Fatal > BestEffort`, `Fatal` the type-level `Default`, duplicate enqueues merging to the stronger claim and an already-failed best-effort target RE-DRIVEN when a later enqueue upgrades it to fatal). Best-effort is opt-in at exactly ONE site — the wholesale sibling-mirror enrolment's `mark_best_effort_source` — and every one of the 7 production `materialise_source_file` call sites derives its provenance by LOOKUP rather than by argument, so a call site added later fails safe at `Fatal`. All three tests assert unignored with no assertion touched; per the epic's corrected flip protocol these are unit/self-skipping tests, so the attribute was deleted outright rather than swapped for an `env-gate:` tag. Four new non-ignored tests joined `bundler.rs` in the same commit: the lattice merge in both orders, the fail-safe `Fatal` default for every unmarked source, a best-effort-only drain staying green, and the epic-mandated "best-effort first, fatal later" upgrade-and-retry proof.

The table below lists all **67** ignored Rust tests (**52** `env-gate`, **7** `heavy`, **1** `verification`, **7** `pending-feature`) and their scheduled homes. Update this table whenever a `#[ignore]` is added, removed, or reclassified. Re-measure rather than trusting the stated total: `grep -rn '#\[ignore = "' crates/ | grep -v ':[0-9]*: *//' | wc -l`, and the same pipeline with `| grep -o '#\[ignore = "[a-z-]*:' | sort | uniq -c` for the per-tag split. The `grep -v` guards against a `//` comment quoting the attribute text (as one did between #1983 and #1985).

| Test (file:line) | Tag | Where / how it runs |
|---|---|---|
| `crates/zfb-islands/tests/integration.rs:226` `subprocess_bundler_against_real_binary` | `env-gate` (esbuild) | **CI**: `health.yml` → "Run zfb-islands esbuild-gated integration suite" step (runs right after the esbuild staging/assert step). Local: `ZFB_ESBUILD_BIN=<abs path> cargo test -p zfb-islands -- --ignored`. |
| `crates/zfb-islands/tests/integration.rs:289` `shared_bundle_keeps_islands_with_no_top_level_side_effect` | `env-gate` (esbuild) | Same as above. |
| `crates/zfb-islands/tests/integration.rs:416` `stage_escape_audit_rejects_workspace_package_symlink_escape` | `env-gate` (esbuild) | Same CI step as above (not a separately-wired health.yml step). Local: `ZFB_ESBUILD_BIN=<abs path> cargo test -p zfb-islands --test integration -- --ignored --exact stage_escape_audit_rejects_workspace_package_symlink_escape`. Issue #1708 (Stage Escape Guards epic #1702, Wave 4 confirm pass): drives `EsbuildSubprocessBundler` directly (below guard (a), which is a scan-time check in the zfb command layer and never runs in this crate) against a genuine pnpm-workspace-style `node_modules` symlink to a sibling package outside `node_modules`; REAL esbuild resolves the bare import and produces a REAL metafile, and guard (b)'s `StageAuditPolicy` audit (issue #1705) rejects it. Verified: without `--preserve-symlinks`, esbuild reports the input as a `..`-climbing relative path (audit "case 4" — no staged spelling ever existed), not a `node_modules/…`-shaped key (case 2) — both cases already have unit coverage in `zfb-build`; this test's job is proving a real esbuild run lands in the audited set. |
| `crates/zfb-islands/tests/integration.rs:509` `stage_escape_audit_accepts_staged_symlink_through_symlink_aliased_working_dir` | `env-gate` (esbuild) | Same CI step as above (not a separately-wired health.yml step). Local: `ZFB_ESBUILD_BIN=<abs path> cargo test -p zfb-islands --test integration -- --ignored --exact stage_escape_audit_accepts_staged_symlink_through_symlink_aliased_working_dir`. Issue #1796 (Stage Audit Fix epic #1794, Wave 2): the ACCEPT half of guard (b)'s env-gated coverage, beside the rejection test above. Real esbuild's `working_dir` is set to a symlink alias one component SHALLOWER than its physical location (`alias -> physical/real`, the same depth-changing topology as the Wave-1 unit repro `stage_escape_allows_staged_symlink_reached_through_symlink_aliased_cwd` in `crates/zfb-build/src/metafile_deps.rs`), and the island source is a symlink inside the stage pointing at live first-party source outside it, with `BundleConfig::with_preserve_symlinks(true)` explicitly set. Verified red before issue #1795's discriminator fix (temporarily disabling `logical_path_names_staged_entry`'s call site reproduces the exact `../../../alias/app/components/Counter.tsx` misclassified-as-case-4 rejection the epic describes) and green after. |
| `crates/zfb-islands/tests/integration.rs:583` `splitting_emits_chunk_for_dynamic_import` | `env-gate` (esbuild) | Same as above. |
| `crates/zfb-islands/tests/integration.rs:697` `no_dynamic_import_yields_single_file` | `env-gate` (esbuild) | Same as above. |
| `crates/zfb-islands/tests/integration.rs:746` `shared_bundle_discovers_marked_glue_and_wasm_resources` | `env-gate` (esbuild) | Same as above. |
| `crates/zfb-islands/tests/integration.rs:830` `client_script_real_esbuild_bundles_discovered_entry` | `env-gate` (esbuild) | Same as above. |
| `crates/zfb-islands/tests/integration.rs:935` `client_script_shadow_jobs_resolve_project_tsconfig_aliases` | `env-gate` (esbuild) | Same as above. |
| `crates/zfb-islands/tests/integration.rs:1046` `islands_shadow_raw_import_bundles_text` | `env-gate` (esbuild) | Same as above. |
| `crates/zfb-islands/tests/integration.rs:1107` `islands_shadow_alias_raw_import_bundles_text` | `env-gate` (esbuild) | Same as above. |
| `crates/zfb-islands/tests/integration.rs:1190` `client_script_raw_import_bundles_text` | `env-gate` (esbuild) | Same as above. |
| `crates/zfb-islands/tests/integration.rs:1255` `island_module_worker_emits_contract_companion_and_dev_layout` | `env-gate` (esbuild) | Same as above. |
| `crates/zfb-islands/tests/integration.rs:1409` `module_worker_define_only_change_updates_query_and_emitted_bytes` | `env-gate` (esbuild) | Same as above. |
| `crates/zfb-islands/tests/integration.rs:1469` `module_worker_package_config_switch_updates_query_and_emitted_bytes` | `env-gate` (esbuild) | Same as above. |
| `crates/zfb-islands/tests/integration.rs:1561` `module_worker_plugin_inputs_update_query_closure_and_emitted_bytes` | `env-gate` (esbuild) | Same as above. |
| `crates/zfb-islands/tests/integration.rs:1667` `island_css_import_bundles_without_error` | `env-gate` (esbuild) | Same as above. |
| `crates/zfb-islands/tests/integration.rs:1729` `island_module_css_import_bundles_without_error` | `env-gate` (esbuild) | Same as above. |
| `crates/zfb-islands/tests/integration.rs:2031` `islands_shadow_expands_glob_and_executes` | `env-gate` (esbuild) | Same as above. |
| `crates/zfb-islands/tests/integration.rs:2102` `islands_shadow_preserve_symlinks_is_load_bearing` | `env-gate` (esbuild) | Same as above. |
| `crates/zfb-islands/tests/integration.rs:2159` `working_dir_is_clean_after_multi_entry_bundle` | `env-gate` (esbuild) | Same as above (the health.yml step runs `-- --ignored` across the whole crate). Local: `ZFB_ESBUILD_BIN=<abs path> cargo test -p zfb-islands --test integration -- --ignored --exact working_dir_is_clean_after_multi_entry_bundle`. Issue #1976 (Sweep Cleanups epic #1975, fixing #1970): the acceptance test for the `.zfb-esbuild-entry-*.tsx` leak. Drives `bundle_per_island` (genuinely multi-entry — one `bundle_one_entry` per island plus the runtime pass) followed by the production-wired shared `bundle()`, against a project root that also carries an entry stranded by a previous zfb process killed mid-bundle, and asserts the root ends clean. The `NamedTempFile` Drop already covered the success path; what this pins is `sweep_orphaned_in_project_entries` reaping what non-unwinding termination leaves behind. The complementary safety half — an entry a concurrent `zfb dev` may still own is spared, both when it is merely *recent* and when it is old but still holds the shared lock `bundle_one_entry` takes on its own entry — is unit-tested in `crates/zfb-islands/src/esbuild.rs` and needs no esbuild. |
| `crates/zfb/src/commands/build.rs:11192` `preprocessing_shadows_bundle_nested_alias_raw_and_workers_with_real_esbuild` | `env-gate` (esbuild) | **CI**: `health.yml` → "Run zfb command-layer env-gate unit tests (commands::build)". Local: `ZFB_ESBUILD_BIN=<abs path> cargo test -p zfb --lib commands::build::tests::preprocessing_shadows_bundle_nested_alias_raw_and_workers_with_real_esbuild -- --ignored`. |
| `crates/zfb/src/commands/build.rs:11779` `plugin_alias_only_client_preprocessing_triggers_shadow_with_real_esbuild` | `env-gate` (esbuild) | Same CI step as above. Local: `ZFB_ESBUILD_BIN=<abs path> cargo test -p zfb --lib commands::build::tests::plugin_alias_only_client_preprocessing_triggers_shadow_with_real_esbuild -- --ignored`. |
| `crates/zfb/tests/client_bundling_cross_pipeline.rs:1338` `real_binary_build_and_dev_cover_client_bundling_contract` | `env-gate` (esbuild) | **CI**: `health.yml` → "Run zfb client-bundling cross-pipeline acceptance test". Local: `ZFB_ESBUILD_BIN=<abs path> cargo test -p zfb --test client_bundling_cross_pipeline -- --ignored`. Level-4 real `zfb build` + `zfb dev --port 0`; serialized by the flock, the in-binary serial guard, and nextest `e2e-heavy` group. |
| `crates/zfb/tests/client_bundling_cross_pipeline.rs:1363` `real_binary_build_and_dev_cover_pnpm_workspace_raw_hardening_contract` | `env-gate` (esbuild) | Same CI step as above. Local: `ZFB_ESBUILD_BIN=<abs path> cargo test -p zfb --test client_bundling_cross_pipeline -- --ignored`. Level-4 real `zfb build` + `zfb dev --port 0` confirmation for issue #1620's pnpm-workspace raw-hardening fixture; serialized by the flock, the in-binary serial guard, and nextest `e2e-heavy` group. |
| `crates/zfb/tests/client_bundling_cross_pipeline.rs:1390` `real_binary_build_and_dev_cover_workspace_reroot_host_contract` | `env-gate` (esbuild) | Same CI step as above (the step runs `-- --ignored`, which picks up every ignored test in the binary). Local: `ZFB_ESBUILD_BIN=<abs path> cargo test -p zfb --test client_bundling_cross_pipeline -- --ignored`. Level-4 real `zfb build` + `zfb dev --port 0` for issue #1674's genuinely-claimed pnpm-workspace sub-package host reaching sibling `?raw` + worker preprocessing; serialized by the flock, the in-binary serial guard, and nextest `e2e-heavy` group. Extended in #1701 (Wave 2 confirm pass): the host's client entry also imports a registered VIRTUAL MODULE whose own source absolute-imports a third sibling package (`sub-packages/vmodule-sibling`), which exercises the client-supported sibling rewrite kinds — `?raw` + a nested module worker — proving the workspace tier of `remap_virtual_module_project_imports_to_shadow` (#1699/#1700/#1701) carries those preprocessing passes through a virtual-module edge, not just a plain relative sibling import. The confirm pass surfaced that the command-layer client-preprocess flow (`crates/zfb/src/commands/build.rs`) is a SECOND bundling path that Wave 1's `run_esbuild`-only remap missed; #1701 closes it by remapping the virtual sources at both client-script sites. `import.meta.glob` is intentionally NOT covered through this CLIENT edge — client scripts do not expand glob (issue #1404); glob through a virtual-module sibling is an SSR/bundler-path concern. Extended again in #1758 (Stage Audit Fix epic #1794, Wave 3) to cover the ISLANDS edge: the host page also renders a `"use client"` island (`RerootIsland.tsx`) that imports the same `virtual:reroot-vmodule-host` virtual module, proving `remap_project_plugin_virtual_modules_to_shadow` (reached via `build_default_islands_payload`) carries the sibling `?raw` + nested-module-worker rewrites through the islands bundler too — a THIRD parallel esbuild flow alongside SSR and client-script preprocessing. The islands pipeline emits its own copy of the shared vmodule worker companion into the islands namespace (`dist/assets/`), distinct from the client-script copy in `dist/assets/client/`. This extension was blocked until #1795 fixed a stage-escape audit false positive that rejected any project-local island source in this workspace-widened host (issue #1781); `import.meta.glob` stays out of scope for the same #1404 reason as the client edge. |
| `crates/zfb/tests/client_bundling_cross_pipeline.rs:1567` `real_binary_build_and_dev_expand_sibling_alias_macros_under_workspace` | `env-gate` (esbuild) | Same CI step as above. Local: `ZFB_ESBUILD_BIN=<abs path> cargo test -p zfb --test client_bundling_cross_pipeline -- --ignored`. Issue #1989 (epic #1982, Wave 7 confirm pass): a real `zfb build` + `zfb dev --port 0` reproduction of the #1724/#1985 fix at the full-pipeline level (the lower-level real-esbuild proof lives in `bundler_sibling_mirror_esbuild_regression.rs`'s cases (b)/(g)/(j)/(k)). A bare (undeclared, non-package) sibling directory `lib/shared` under a `pnpm-workspace.yaml`-claimed root is reached only through a tsconfig alias from `host/app`, and carries its OWN `import.meta.glob` (an eager glob over sibling-local files) and module-worker `new URL(...)` macro. Asserts both the production `dist/index.html` and the development page served over HTTP: the glob-matched items' values reach the page (proving expansion, not a raw copy — an unexpanded `import.meta.glob(` would either throw at V8 render time or fail the whole build), and the worker macro's REWRITTEN `<slug>.js?v=<hash>` specifier (never the raw `./inner.worker.ts` path) is visible via `String(makeWorker)` rendered into the page — a safe way to inspect the bundled function's rewritten source text without ever constructing a real `Worker` during SSR. |
| `crates/zfb/tests/client_bundling_cross_pipeline.rs:1914` `real_binary_build_and_dev_accept_root_claimed_declared_sibling` | `env-gate` (esbuild) | Same CI step as above. Local: `ZFB_ESBUILD_BIN=<abs path> cargo test -p zfb --test client_bundling_cross_pipeline -- --ignored`. Issue #1989 (epic #1982, Wave 7 confirm pass): the POSITIVE control for the root-claimed-workspace topology (`packages: ['.', 'packages/*']`, so `first_party_root_for(project_root) == project_root` — #1730's blind spot). `@acme/ui` declares `./cta` as a real `exports` entry root and ships no `dist` — #2040's "consume from source" carve-out — reached by bare package name from both the SSR page and a "use client" island via an ordinary import. A real `zfb build` + `zfb dev --port 0` must still succeed at this topology now that #1987/#1988 arm guard (b) here, guarding against the armed audit becoming an overzealous "workspace siblings are never fine" regression. Because this project owns its own `node_modules` (for the `@acme/ui` symlink), the embedded-vendor SSR runtime fallback (`commands::bundler_input`) never activates, so the fixture also symlinks the monorepo's own real, already-installed `preact` / `preact-render-to-string` (resolved via the first matching `node_modules/.pnpm/preact@*`/`preact-render-to-string@*` entry, never a pinned version string) and the `@takazudo/zfb-runtime` workspace package (`packages/zfb-runtime`, whose `package.json` `exports` point straight at TS source, no build step needed) into place by hand. |
| `crates/zfb/tests/client_bundling_cross_pipeline.rs:1960` `real_binary_build_rejects_root_claimed_undeclared_sibling_escape_on_both_legs` | `env-gate` (esbuild) | Same CI step as above. Local: `ZFB_ESBUILD_BIN=<abs path> cargo test -p zfb --test client_bundling_cross_pipeline -- --ignored`. Issue #1989 (epic #1982, Wave 7 confirm pass): the NEGATIVE control paired with the positive row above — same root-claimed-workspace topology, but `@scope/child` is UNDECLARED (no `exports`/`main`) and reached only via the wholesale `node_modules/@scope/child -> packages/child` symlink a real pnpm install produces. Drives TWO separate real `zfb build` invocations (never combined into one project, so a build failing on whichever leg runs first can't mask the other leg never being reached): one where `pages/index.tsx` reaches the escape via an ordinary `import` (SSR has no scan-time guard (a) at all, so this alone must be caught by guard (b) — the `bundler_root_workspace_stage_escape_audit_armed_regression.rs` shape, confirmed here through the full CLI instead of `bundle_with_session`); one where a `"use client"` island reaches it via a plain `require(...)` call, the scanner-missed edge shape guard (a) never records (the `commands::build.rs` `root_workspace_islands_stage_escape_audit_is_now_armed_and_undeclared_child_escape_is_rejected` shape, likewise confirmed through the full CLI). Both invocations must exit non-zero with the rejection naming `@scope/child`. |
| `crates/zfb/tests/wasm_ssr_adapter_e2e.rs:375` `wrangler_dry_run_attaches_runtime_wasm_as_compiled_module` | `env-gate` (wrangler 4.85.0) | Local: `ZFB_ESBUILD_BIN=<abs path> cargo test -p zfb --test wasm_ssr_adapter_e2e -- --ignored`. **CI, weekly (T3):** `exam.yml`'s `quarantine-heavy` job runs it via the exact-name filterset (issue #1344). Level-4 real `zfb build` plus pinned `wrangler deploy --dry-run --no-bundle --outdir`, using Wrangler's default `CompiledWasm` rule; it confirms the exact copied SSR-only Wasm import reaches the module table as `compiled-wasm` with matching emitted bytes, and skips when Wrangler is unavailable. Serialized by nextest `e2e-heavy`. |
| `crates/zfb/tests/workerd_parity_e2e.rs:557` `embedded_host_matches_real_workerd_for_supported_rows_and_documented_divergences` | `env-gate` (wrangler 4.85.0) | Local: `ZFB_ESBUILD_BIN=<abs path> cargo test -p zfb --test workerd_parity_e2e -- --ignored`. **CI, weekly (T3)**: `exam.yml`'s `quarantine-heavy` job runs it via the exact-name filterset (issue #2020). Issue #2020 (V8 Request Time Parity epic #2012, Wave 8 — the central confirm pass over Waves 1-7): a real `zfb build` (`@takazudo/zfb-adapter-cloudflare` adapter) followed by a real, workspace-pinned `wrangler dev` (real workerd, local mode — no `--remote`, no account, no public internet) serving `tests/fixtures/workerd-parity/pages/api/{happy,legacy-digest,keyed-crypto}.tsx`, and a real `zfb dev` serving the SAME page sources unmodified through the embedded V8 host. `/api/happy` (fetch + `getRandomValues` + `randomUUID` + `subtle.digest("SHA-256", ...)`) must succeed identically on both runtimes, down to a byte-identical digest hex for the same fixed input — the strongest available proof that a contract row marked "supported" means the same thing in both places. `/api/legacy-digest` (divergence D7) must succeed on real workerd with the RFC 1321 MD5 test vector for `"abc"` and fail closed on the embedded host with `NotSupportedError`. `/api/keyed-crypto` (divergence D8) drives a genuine AES-GCM generateKey→encrypt→decrypt round trip: real workerd must recover the exact plaintext (proof of real support, not just "didn't throw"), while the embedded host must fail closed with `NotSupportedError` naming the zfb embedded runtime. Skips cleanly (not a pass) when wrangler, esbuild, or pnpm are unavailable. Flock-adopting; serialized by nextest `e2e-heavy`. |
| `crates/zfb-css/tests/integration.rs:51` `subprocess_engine_against_real_binary` | `env-gate` (tailwindcss v4) | Local: `cargo test -p zfb-css --test integration -- --include-ignored`. **CI, T1 (issue #1393)**: `health.yml` → "Run zfb-css tailwindcss-v4 env-gate integration test" step. **Also CI, weekly (T3)**: `exam.yml`'s `quarantine-heavy` job runs it via the exact-name filterset (issue #1344). |
| `crates/zfb-build/tests/prod_asset_graph_e2e.rs:798` `prod_asset_graph_with_real_tailwind_binary_against_fixture` | `env-gate` (tailwindcss v4) | Local: `cargo test -p zfb-build --test prod_asset_graph_e2e -- --include-ignored`. **CI, T1 (issue #1393)**: `health.yml` → "Run zfb-build tailwindcss-v4 env-gate integration test" step. **Also CI, weekly (T3)**: `exam.yml`'s `quarantine-heavy` job (issue #1344). |
| `crates/zfb-build/tests/bundler_dot_path_staging_esbuild_regression.rs:195` `real_esbuild_resolves_dot_path_staged_spellings_and_passes_stage_escape_audit` | `env-gate` (esbuild) | Local: `ZFB_ESBUILD_BIN=<abs path> cargo test -p zfb-build --test bundler_dot_path_staging_esbuild_regression -- --ignored`. **CI**: `health.yml` → "Run zfb-build dot-path staging esbuild regression test" step. Issue #1841 (epic #1836, confirming #1840): the mock `bundler_dot_path_staging.rs` suite drives every fixture through `mock_subprocess_output`, so the stage-escape audit (`workspace_rel.is_some()`, #1706) — which needs a real esbuild metafile — never actually runs there. This test drives a real `bundle_with_session` for a pnpm-workspace project whose `pages/index.tsx` reaches a `.zudo-doc/routes-src/generated-route.tsx` route module through a tsconfig alias, which itself reaches `.zfb/doc-history-meta.json` through the same `#doc-history-meta`-alias-to-`.zfb/doc-history-meta.json` shape zudo-doc's generated route module has historically used; asserts both markers reach the emitted bundle AND that both dot-path files' STAGED spellings (not a live-tree escape) appear as metafile input keys. Verified red before restoring the fix: temporarily emptying `KNOWN_FIRST_PARTY_STAGING_DIRS`/`KNOWN_FIRST_PARTY_STAGING_JSON_DIRS` to `&[]` reproduces the exact case-4 "no staged spelling" stage-escape failure for both files (see the test file's own header comment for the observed error text). |
| `crates/zfb-build/tests/bundler_workspace_root_alias_esbuild_regression.rs:133` `real_esbuild_stages_workspace_root_alias_graph_with_sibling_and_dot_paths`; `:282` `real_esbuild_combines_all_next_92_residual_stage_escape_classes`; `:508` `parent_relative_root_tree_crossing_stays_rejected_even_when_alias_target_is_legal`; `:558` `real_metafile_guard_b_rejects_genuinely_unstaged_workspace_input` | `env-gate` (esbuild) | Local: `ZFB_ESBUILD_BIN=<abs path> cargo test -p zfb-build --test bundler_workspace_root_alias_esbuild_regression -- --ignored`. **CI**: `health.yml` → "Run zfb-build workspace-root alias esbuild regression test" step. Issue #1905 extends #1885's real-esbuild pnpm-workspace consumer topology with the adoption report's exact 17 residual inputs: six `.zudo-doc/routes-src` package-owned route sources, nine public `workspace:*` package-name subpath exports, and two gitignored JSON leaves reached through the claimed root alias. It asserts exact staged metafile spellings for all 17 (plus the CSS input separately), never canonical live paths. The serial real `zfb build` companion in `build_package_routes_consumer.rs` renders those route/root/package classes through V8 and checks the established emitted CSS artifact. Separate negatives preserve parent-relative root-tree rejection and guard (b)'s fail-closed response to a genuinely unstaged hidden root input; neither negative payload is used in positive absence assertions. |
| `crates/zfb-build/tests/bundler_consume_from_source_esbuild_regression.rs:204` `nested_member_consume_from_source_sibling_is_accepted_as_declared_first_party_source`; `:255` `nested_member_undeclared_deep_import_into_dist_shipping_sibling_stays_rejected`; `:309` `root_project_consume_from_source_sibling_builds_clean_today_for_the_identical_shape` | `env-gate` (esbuild) | Local: `ZFB_ESBUILD_BIN=<abs path> cargo test -p zfb-build --test bundler_consume_from_source_esbuild_regression -- --ignored`. **CI**: `health.yml` → "Run zfb-build consume-from-source esbuild regression test" step. Sourced from #1730's second comment: a first-party sibling package (`@acme/ui`) consumed from source (`exports` -> `./src/*`, no `dist`) is imported by bare package name (`@acme/ui/cta-button`, `@acme/ui/theme-state`) via a workspace-hoisted `node_modules/@acme/ui` symlink, landing exactly on `metafile_deps.rs`'s documented case-2 shape. The first test landed RED under issue #2039 (Wave 1, then named `…_hard_fails_as_case_two_offender_today`) and was **flipped green by issue #2040** (Wave 4), which redefined case 2: a case-2-shaped input is accepted when the target package's own `package.json` declares that location as an entry root and `pnpm-workspace.yaml` claims the package. The second test proves the workspace **ROOT** project builds clean for the byte-identical import shape; today that is still an accident of `first_party_root == project_root` disabling the widened-stage guard (the #1730 blind spot), but once #1987/#1988 arm the audit there it holds on the merits instead — this green result must be **preserved**, never re-derived. The third test is #2040's own negative and is what stops the new rule from being a blanket "workspace siblings are always fine" exemption: a sibling that DOES ship a built `dist` and declares only `./dist/index.js` has its declared dist entry accepted while a deep import climbing past it into the undeclared `src/internal.ts` stays rejected. Both of that test's imports name a path inside the package rather than the bare package name — zfb drives esbuild with the `neutral` platform, which ignores `main`, so `import "@acme/built"` would not resolve at all; the keys are still `node_modules/@acme/built/...`, i.e. case-2 shaped. |
| `crates/zfb-build/tests/bundler_consume_from_source_esbuild_regression.rs:290` `nested_member_consume_from_source_sibling_accepted_via_canonicalized_key_under_copy_mode` | `pending-feature: https://github.com/Takazudo/zudo-front-builder/issues/2086` | Local: `ZFB_ESBUILD_BIN=<abs path> cargo test -p zfb-build --test bundler_consume_from_source_esbuild_regression -- --ignored --exact nested_member_consume_from_source_sibling_accepted_via_canonicalized_key_under_copy_mode`. **CI**: intentionally NOT run yet — `health.yml`'s "Run zfb-build consume-from-source esbuild regression test" step blanket-runs `-- --ignored` for this whole binary, so its invocation now also carries `--skip nested_member_consume_from_source_sibling_accepted_via_canonicalized_key_under_copy_mode` (see the step's own comment) to keep the three pre-existing env-gate tests green without this still-red test dragging the step down. Issue #2082 (epic #2078 Wave 1, serving #2047): the SAME #2040 consume-from-source topology as the row above, but under copy_mode (`node_modules_dir` set + non-empty `tsconfig_paths`, no `--preserve-symlinks` — `esbuild_will_preserve_symlinks`, bundler.rs :10153/:2019) esbuild canonicalises the `node_modules/@acme/ui` symlink back to a `..`-climbing relative metafile key instead of a `node_modules/...`-shaped one; `package_shaped` (`has_node_modules_segment` on the KEY STRING) is false for such a key, so the #2040 declared-entry exemption never fires and the build wrongly case-4-rejects a legitimate, declared, claimed sibling. Verified red today: `bundle_with_session` fails with `stage-escape audit — … (first-party input resolved outside every stage root, no staged spelling)` naming the canonicalized `../../../<tmp>/packages/ui/src/...` paths. Sub #2086 flips this to `env-gate: esbuild — …` and drops the `--skip` in the same commit. |
| `crates/zfb-build/src/metafile_deps.rs:1397` `stage_escape_allows_consume_from_source_sibling_via_canonicalized_key` | `pending-feature: https://github.com/Takazudo/zudo-front-builder/issues/2086` | Local only for now: `cargo test -p zfb-build --lib metafile_deps:: -- --ignored --exact stage_escape_allows_consume_from_source_sibling_via_canonicalized_key`. **CI**: none while pending — `metafile_deps.rs`'s unit tests carry no blanket `-- --ignored` health.yml step, so this row needs no `--skip` workaround. Issue #2082 (epic #2078 Wave 1, serving #2047): the unit-level twin of the row above, reusing the #2040 `write_workspace_sibling_stage` fixture but hand-crafting the metafile's `inputs` keys as canonicalized (non-`node_modules`-shaped) paths (`../workspace/packages/ui/src/...`) instead of `node_modules/@acme/ui/...`, isolating the SAME classification gap in `audit_metafile_stage_escape` without a real esbuild subprocess. Verified red today with the identical case-4 message. A permanent (non-ignored) fail-closed twin lives right beside it, `stage_escape_flags_dist_shipping_sibling_canonicalized_key_stays_rejected` — green today and required to stay green after Sub #2086 lands, proving an UNDECLARED deep import reached via a canonicalized key never becomes a blanket "any canonical path under first_party_root is fine" exemption. Sub #2086 deletes this row's `pending-feature` tag entirely (unit test, no external binary) per the epic's corrected flip protocol. |
| `crates/zfb-build/tests/bundler_root_workspace_stage_escape_audit_armed_regression.rs:103` `real_esbuild_now_rejects_root_workspace_child_package_escape_through_node_modules_symlink` | `env-gate` (esbuild) | Local: `ZFB_ESBUILD_BIN=<abs path> cargo test -p zfb-build --test bundler_root_workspace_stage_escape_audit_armed_regression -- --ignored`. **CI**: `health.yml` → "Run zfb-build root-workspace stage-escape audit armed regression test" step. Renamed/reasserted from #1984's RED test (formerly `bundler_root_workspace_stage_escape_audit_disabled_regression.rs`'s `real_esbuild_silently_resolves_root_workspace_child_package_escape_through_node_modules_symlink`): the SSR guard (b) call site (~bundler.rs:3977) now calls `zfb_types::stage_escape_audit_eligibility` (#1986) instead of the bare `workspace_rel.is_some()` proxy, so the same root-claimed-workspace topology (`first_party_root_for(project_root)` still maps to itself) now ARMS the SSR metafile stage-escape audit via row 3 (a reachable `node_modules/@scope/child` link, scanned at `work.join("node_modules")` — the wholesale symlink this site sets up). The child package here is deliberately UNDECLARED (no `exports`/`main`), distinct from #2040's consume-from-source carve-out. `bundle_with_session` now returns `Err` naming the escaped `node_modules/@scope/child/index.ts` input — guard (b) rejects the case-2-shaped, undeclared `@scope/child` import instead of letting it silently resolve through `node_modules` to LIVE source, which is what made this SSR leg P1 (no scan-time guard (a) exists for SSR at all). |
| `crates/zfb-build/tests/bundler_root_workspace_stage_escape_audit_disarm_pin.rs:189` `real_copy_staging_under_active_bundle_exclude_disarms_eligibility_and_ships_the_escape_silently` | `env-gate` (esbuild) | Local: `ZFB_ESBUILD_BIN=<abs path> cargo test -p zfb-build --test bundler_root_workspace_stage_escape_audit_disarm_pin -- --ignored --skip real_copy_staging_under_active_bundle_exclude_should_reject_the_undeclared_escape_once_declared_identity_lands`. **CI**: `health.yml` → "Run zfb-build root-workspace stage-escape audit disarm pin" step (same `--skip` filter, excluding this file's `pending-feature` twin below). Issue #2081 (Staging Correctness 2 epic #2078, Wave 1): pins the KNOWN-DISARMED counterpart to the armed-regression row above — the IDENTICAL undeclared-`@scope/child`-via-symlink fixture, but with an active (non-matching) `bundle.exclude` set. That alone stops the live `<shadow>/node_modules -> <live tree>` symlink from being created at all (`crates/zfb-build/src/bundler.rs` ~:3130-3135); non-excluded dependencies, including the undeclared sibling, are staged as REAL copies at their natural shadow position instead. `stage_escape_audit_eligibility` then finds no symlink evidence at `work.join("node_modules")` and returns `NoReachableFirstPartyPackage`, so guard (b) never arms — `bundle_with_session` returns `Ok`, and the undeclared sibling's marker reaches the emitted bundle unaudited. Asserts the eligibility reason directly (independent of the build's pass/fail outcome) and that `<shadow>/node_modules` is a real directory, not a symlink. |
| `crates/zfb-build/tests/bundler_root_workspace_stage_escape_audit_disarm_pin.rs:266` `real_copy_staging_under_active_bundle_exclude_should_reject_the_undeclared_escape_once_declared_identity_lands` | `pending-feature` (#2087) | Local: `ZFB_ESBUILD_BIN=<abs path> cargo test -p zfb-build --test bundler_root_workspace_stage_escape_audit_disarm_pin -- --ignored --exact real_copy_staging_under_active_bundle_exclude_should_reject_the_undeclared_escape_once_declared_identity_lands`. NOT wired into any CI step (deliberately excluded from the disarm-pin health.yml step via `--skip`, per the pending-feature convention — blocked on unimplemented work, not flakiness). Issue #2081's RED desired-form twin for the row above: once sub #2087 (Wave 3, epic #2078) lands declared-identity eligibility recognition, the SAME configuration must instead reject the escape (`bundle_with_session` returns `Err` naming `node_modules/@scope/child/index.ts`). Written in the assertion form #2087 should make true; per the epic's corrected flip protocol, #2087 must REPLACE this attribute with `#[ignore = "env-gate: esbuild — …"]`, never just delete it. |
| `crates/zfb-build/tests/bundler_root_workspace_stage_escape_audit_disarm_pin.rs:311` `empty_exclude_workspace_package_exact_staging_does_not_disarm_the_audit_at_root_claimed_workspace` | `env-gate` (esbuild) | Local: `ZFB_ESBUILD_BIN=<abs path> cargo test -p zfb-build --test bundler_root_workspace_stage_escape_audit_disarm_pin -- --ignored --exact empty_exclude_workspace_package_exact_staging_does_not_disarm_the_audit_at_root_claimed_workspace`. **CI**: same "Run zfb-build root-workspace stage-escape audit disarm pin" health.yml step as the pin row above (not `--skip`-excluded). Issue #2081's negative control for the SECOND trigger shape #2050's own text named ("empty `bundle.exclude`, but a workspace package is staged"): forces the undeclared `@scope/child` package into `exact_target_staging_dirs` via a plugin alias entry pointing directly at its `node_modules` symlink — the one staging path that runs UNCONDITIONALLY regardless of `bundle.exclude` — while keeping `bundle.exclude` empty. The build still correctly REJECTS the escape (same assertion shape as the armed-regression row above) and the live symlink is confirmed to still exist, because `workspace_package_staging_active`'s own workspace-membership check (`canonical_workspace_package_logical_path`) unconditionally returns `None` whenever `first_party_root == project_root` — the exact root-claimed precondition this whole file requires — so this combination can never independently disarm anything at the root level. A passing negative control, not a bug pin; carries no RED twin. |
| `crates/zfb/src/commands/build.rs:11570` `root_workspace_islands_stage_escape_audit_is_now_armed_and_undeclared_child_escape_is_rejected` | `env-gate` (esbuild) | Local: `ZFB_ESBUILD_BIN=<abs path> cargo test -p zfb --lib commands::build::tests::root_workspace_islands_stage_escape_audit_is_now_armed_and_undeclared_child_escape_is_rejected -- --ignored`. **CI**: `health.yml` → "Run zfb command-layer env-gate unit tests (commands::build)" step, whose `commands::build:: -- --ignored` filter already covers this test — the row previously claimed it was unwired, which was never true. Renamed/reasserted from #1984's RED test (formerly `..._is_disabled_and_child_escape_goes_unflagged`): `stage_escape_audit_policy` (~build.rs:3460) now calls `zfb_types::stage_escape_audit_eligibility` (#1986) instead of the bare widened-stage proxy, so the same root-claimed-workspace topology (`first_party_root_for(project_root)` still maps to itself) now ARMS the islands/client metafile audit via row 3 (a reachable `node_modules/@scope/child` link) — asserted directly first, then confirmed end-to-end. The child package here is deliberately UNDECLARED (no `exports`/`main`), distinct from #2040's consume-from-source carve-out; a "use client" island reaches it through a plain `require(...)` call that guard (a)'s static scanner (`collect_import_edges`, `zfb-islands/src/scanner.rs`) never records as a `WorkspacePackageImportEdge`. `build_default_islands_payload_with_bundle_options` now returns `Err` — guard (b) rejects the case-2-shaped, undeclared `@scope/child` input as a stage escape instead of letting it resolve through `node_modules` to LIVE source unflagged. |
| `crates/zfb/src/commands/build.rs:14120` `default_runner_emit_prod_assets_returns_non_empty_css_for_real_project` | `env-gate` (tailwindcss v4) | Local: `cargo test -p zfb --lib commands::build:: -- --include-ignored`. **CI, T1 (issue #1393)**: `health.yml` → "Run zfb command-layer env-gate unit tests (commands::build)" step. **Also CI, weekly (T3)**: `exam.yml`'s `quarantine-heavy` job (issue #1344). |
| `crates/zfb-css/tests/hi_color_extraction.rs:61` `extract_zfb_hi_colors_from_base16_ocean` | `verification` | Local only: `cargo test -p zfb-css --test hi_color_extraction -- --ignored --nocapture`. One-time color extraction helper for `assets/zfb-hi.css`, not a regression guard. |
| `crates/zfb/tests/version_stamp.rs:23` `version_stamp_from_env` | `heavy` | Local (T4): `cargo test -p zfb --test version_stamp -- --ignored`. Performs a full isolated `cargo run` recompile. **Also CI, weekly (T3)**: `exam.yml`'s `quarantine-heavy` job (issue #1344), allowed-to-fail. |
| `crates/zfb/tests/dev_dep_invalidation_1284_e2e.rs:498` `e2e_src_component_edit_rerenders_route` | `heavy` | Local (T4): `cargo test -p zfb --test dev_dep_invalidation_1284_e2e -- --ignored`. Level-4 e2e, spawns a real `zfb dev` server. **Also CI, weekly (T3)**: `exam.yml`'s `quarantine-heavy` job, allowed-to-fail. |
| `crates/zfb/tests/dev_dep_invalidation_1284_e2e.rs:707` `e2e_transitive_css_import_refreshes_stylesheet` | `heavy` | Same as above. |
| `crates/zfb/tests/dev_dep_invalidation_1284_e2e.rs:908` `e2e_new_utility_class_in_component_is_emitted` | `heavy` | Same as above. |
| `crates/zfb/src/render_pipeline.rs:2360` `eval_deferred_paths_via_worker_embedded_v8_non_literal_paths` | `heavy` | Local (T4): `cargo test -p zfb --lib render_pipeline:: -- --ignored`. Level-4 integration test: boots a real esbuild subprocess + embedded V8 isolate via `crate::v8_host_adapter::ThreadedV8Host`. **Also CI, weekly (T3)**: `exam.yml`'s `quarantine-heavy` job (issue #1344), allowed-to-fail. |
| `crates/zfb/tests/dev_sibling_watch_1678_e2e.rs:357` `e2e_dev_watches_workspace_sibling_raw_and_worker_sources` | `heavy` | Local: `ZFB_ESBUILD_BIN=<abs path> cargo test -p zfb --test dev_sibling_watch_1678_e2e -- --ignored`. **Also CI, weekly (T3)**: `exam.yml`'s `quarantine-heavy` job runs it via the exact-name filterset (issue #1344), allowed-to-fail. Level-4 real `zfb dev --port 0` (issue #1678): proves editing a workspace-sibling `?raw` / module-worker source refreshes the served bundle restart-free, and that a newly-introduced sibling import makes its directory watched (keyed on the `watch-extra registered:` timing signal). Extended in #1711 (Sibling Invalidation epic #1709 confirm pass) with Scenario D: editing a boot-discovered sibling PLAIN module (neither `?raw` nor a worker dependency) also refreshes the served bundle restart-free, proving the `client_script_siblings` registry (#1710) closes the dev loop for that third sibling shape. Flock-adopting; serialized by the nextest `e2e-heavy` group. |
| `crates/zfb/tests/dev_sibling_watch_1678_e2e.rs:642` `e2e_dev_sibling_tailwind_utility_class_refreshes_served_css` | `env-gate` (tailwindcss v4 + esbuild) | Local: `ZFB_ESBUILD_BIN=<abs path> ZFB_TAILWIND_BIN=<abs path> cargo test -p zfb --test dev_sibling_watch_1678_e2e -- --ignored --exact e2e_dev_sibling_tailwind_utility_class_refreshes_served_css` (ZFB_TAILWIND_BIN or the staged `crates/zfb/binaries/tailwindcss-v4` slot). **Also CI, weekly (T3)**: `exam.yml`'s `quarantine-heavy` job runs it via the exact-name filterset (issue #1344). Issue #1805 (Tailwind Sibling Source epic #1799, Wave 3 confirm pass, gap (a)): a SEPARATE `#[tokio::test]` fn in the SAME binary as the `heavy` row above (not a fifth scenario in that function — see the file's own header comment) proving the DEV-LOOP half of the `css_mirror_roots` recursive-directory watch (#1801/#1802) that `sibling_only_utility_class_reaches_tailwind_source_scan_and_is_emitted` below proves for `zfb build`: a fresh pnpm-workspace fixture whose sibling (`lib/ushared`) is claimed ONLY through a tsconfig alias (never imported by any client/worker/plain-module source, unlike this file's `sub/shared`, which is ALSO reached by Scenarios A/B/D's imports and so would not falsify) gets a NEW Tailwind utility class (`bg-[#ff6ad5]`) added to `Badge.tsx` mid-session; asserts the compiled bytes reach `GET /assets/styles.css` without a `zfb dev` restart, keyed on the `watch-extra registered:` signal (shared with the `heavy` row above) rather than a sleep. Carries its own project-level `.gitignore` excluding `.zfb-build/` (same confound as `sibling_css_module_command_layer_build.rs:649-662` — dev's own SSR bundle under `.zfb-build/` would otherwise leak the class into Tailwind's automatic content detection). Verified to fail (timeout on `edit_until_served`, no filesystem event ever observed for `lib/ushared`) when `orchestrator.rs`'s `register_dynamic_dependency_watches` is reverted to skip `Watcher::sync_recursive_dir_watches`. |
| `crates/zfb/tests/sibling_css_module_command_layer_build.rs:935` `sibling_only_utility_class_reaches_tailwind_source_scan_and_is_emitted` | `env-gate` (tailwindcss v4 + esbuild) | Local: `ZFB_ESBUILD_BIN=<abs path> cargo test -p zfb --test sibling_css_module_command_layer_build -- --ignored --exact sibling_only_utility_class_reaches_tailwind_source_scan_and_is_emitted` (ZFB_TAILWIND_BIN or the staged `crates/zfb/binaries/tailwindcss-v4` slot). **Also CI, weekly (T3)**: `exam.yml`'s `quarantine-heavy` job runs it via the exact-name filterset (issue #1344). Issue #1776 (Tailwind Sibling `@source` epic, Wave 2): a real `zfb build` for a pnpm-workspace host whose sibling component (reached through a claimed tsconfig alias) uses a utility class (`bg-[#1a2b3c]`) that appears NOWHERE under `project_root`; asserts the compiled utility's bytes land in the emitted `styles-<hash>.css`, proving `SiblingMirrorPlan` mirror roots feed `assemble_css_content_globs` / Tailwind's own `@source` content-glob scan (`crates/zfb/src/commands/build.rs`), not just the CSS Modules source walk `discover_css_source_files` already covered. The fixture ships its own `.gitignore` excluding `.zfb-build/`/`dist/`/`node_modules/` — Tailwind v4's default automatic content detection respects `.gitignore`, and without it the intermediate `.zfb-build/bundle.mjs` esbuild writes before the CSS pass would leak the sibling's class string into auto-detection and mask whether the `@source` wiring under test did anything (confirmed by cherry-picking the fix out: the test fails without it, passes with it). |
| `crates/zfb/tests/sibling_css_module_command_layer_build.rs:1055` `sibling_generated_dir_utility_class_is_excluded_from_tailwind_source_scan` | `env-gate` (tailwindcss v4 + esbuild) | Local: `ZFB_ESBUILD_BIN=<abs path> cargo test -p zfb --test sibling_css_module_command_layer_build -- --ignored --exact sibling_generated_dir_utility_class_is_excluded_from_tailwind_source_scan` (ZFB_TAILWIND_BIN or the staged `crates/zfb/binaries/tailwindcss-v4` slot). **Also CI, weekly (T3)**: `exam.yml`'s `quarantine-heavy` job runs it via the exact-name filterset (issue #1344). Issue #1803 (Tailwind Sibling Source epic #1799 Wave 2, gap b): extends `write_utility_only_sibling_fixture` with an ungitignored `dist/`-style generated directory inside the sibling mirror root (`lib/ushared/dist/`) carrying its own arbitrary-value utility class (`bg-[#9f8e7d]`) found NOWHERE else in the fixture; asserts that class is ABSENT from the emitted `styles-<hash>.css` while the sibling's legitimate `bg-[#1a2b3c]` utility (outside that subtree) REMAINS PRESENT, proving `assemble_css_content_globs`'s new `negative_source_globs` output (`crates/zfb/src/commands/build.rs`) selectively excludes `CSS_SIBLING_MIRROR_SKIP_DIRS` infra dirs from Tailwind's `@source` content-glob scan at any depth, rather than merely breaking sibling scanning wholesale. The sibling carries no `.gitignore` of its own (only the project's `.gitignore` covers `.zfb-build/`/`dist/`/`node_modules/` inside the project, not the workspace-sibling mirror root), so Tailwind's `.gitignore`-respecting automatic content detection does not save this test from the leak on its own (confirmed by cherry-picking the fix out: the test fails — the leak class present — without it, and passes with it). |
| `crates/zfb/tests/mirror_css_scan_mdx_e2e.rs:348` `sibling_mdx_utility_class_reaches_dev_css_scan` | `env-gate` (tailwindcss v4 + esbuild) | Local: `ZFB_ESBUILD_BIN=<abs path> ZFB_TAILWIND_BIN=<abs path> cargo test -p zfb --test mirror_css_scan_mdx_e2e -- --ignored` (or the staged `crates/zfb/binaries/` slots). **Also CI, weekly (T3)**: `exam.yml`'s `quarantine-heavy` job runs it via the exact-name filterset (issue #1998). Mirror Root CSS Scan epic #1995: written RED in Wave 1 (#1996, then `mirror_css_scan_mdx_red_e2e.rs` / `red_sibling_mdx_utility_class_never_reaches_dev_css_scan`) and **inverted in Wave 2 (#1997)**, which implemented option (b) for #1819 — a real `zfb dev` session over a sibling mirror root claimed ONLY via a tsconfig alias now sees a `.mdx` utility class edited mid-session reach `GET /assets/styles.css` restart-free. Delivery is asserted separately first (the `tick(): kinds=[notes.mdx:...]` timing line), so a watcher regression can never be misread as a CSS-rerun regression. Deliberately NOT reusing `dev_sibling_watch_1678_e2e.rs`'s `sub/shared`, which is claimed by BOTH dynamic-watch channels and would pass with the fix reverted. Fixture ships its own `.gitignore` excluding `.zfb-build/` so dev's intermediate bundle cannot leak the class into Tailwind auto-detection. Flock-adopting; in the nextest `e2e-heavy` group. The cheap unit-level counterparts (in-root negative, registry-population proof, removed-path fold, `External` arm, hook arm) live in `zfb-build`'s `orchestrator.rs`/`policy.rs` and `zfb`'s `commands/dev.rs` and run on every gate. **Revert-proof (issue #1998, Wave 3)**: `zfb-build/src/orchestrator.rs`'s `content_under_css_mirror_root` was temporarily forced to `return false` unconditionally; the test then failed with `/assets/styles.css never picked up the sibling-.mdx-only utility class within 60s of the edit` even though the preceding `tick(): kinds=[notes.mdx:Created]` line proved the filesystem event still reached the orchestrator — proof the CSS-rerun gate, not delivery, is what the test exercises. Reverted back and confirmed clean (`git diff` empty) before committing. |
| `crates/zfb/tests/cold_rewrite_prewarm_e2e.rs:404` `cold_redirects_rewrite_target_is_prewarmed_and_resolves` | `heavy` | Local: `ZFB_ESBUILD_BIN=<abs path> cargo test -p zfb --test cold_rewrite_prewarm_e2e -- --ignored`. **CI, weekly (T3)**: `exam.yml`'s `quarantine-heavy` job runs it via the exact-name filterset, wired in issue #2007 (Dev Self Heal epic #1999 Wave 6, the central gate). Dev Self Heal epic #1999, for issue #1825: written RED in Wave 1 (#2001, then `cold_rewrite_red_e2e.rs` / `cold_redirects_rewrite_target_404s_indefinitely`) and **inverted in Wave 4 (#2004)**, which landed the pre-warm. A real `zfb dev` E2E over a fresh Cold (`ZFB_DEV_BOOT_LAZY=cold`) boot with `public/_redirects` containing `/alias /target 200`; asserts `GET /alias` resolves the rewrite target, polling repeatedly and interleaving each poll with a genuine, SSE-confirmed unrelated watcher tick (an edit to the fixture's unrelated `pages/index.tsx` — never `/target`). Wave 1 wrote the assertion in its DESIRED post-fix form, so Wave 4 flipped it green without touching a single assertion — only the file, fn, fixture dir, marker strings, and prose were renamed. `/target` is never requested directly at any point: doing so would render it via the (different, legitimate) render-on-request hook dispatch path and would pass even with the pre-warm removed. **Revert-proof (#2004)**: wrapping the boot hook's `wiring.run(&redirects_handle_for_boot)` call in `if false` reproduces the exact pre-fix symptom — all 4 polls return the dev 404 body with the 3 unrelated ticks still confirmed — proving the test exercises the pre-warm, not incidental rendering. The complementary contract (a rewrite re-runs NO plugin dev-middleware, embed handler, SSR dispatch, or render-on-request hook) is pinned separately and cheaply by `rewrite_reruns_no_middleware_embed_ssr_or_render_hook` in `crates/zfb-server/src/routes.rs`, which runs on every gate. In the nextest `e2e-heavy` group (flock-adopting via `CrossBinaryE2eLock`), registered in issue #2007. |
| `crates/zfb/tests/ssr_reload_self_heal_e2e.rs:400` `ssr_only_healthy_deferred_publish_reloads_open_tab`; `:531` `ssr_only_cold_bootstrap_recovery_reloads_open_tab` | `env-gate` (esbuild) | Local: `ZFB_ESBUILD_BIN=<abs path> cargo test -p zfb --test ssr_reload_self_heal_e2e -- --ignored` (or the staged `crates/zfb/binaries/esbuild` slot). **CI, weekly (T3)**: `exam.yml`'s `quarantine-heavy` job runs both via the exact-name filterset, wired in issue #2007 (Dev Self Heal epic #1999 Wave 6, the central gate) — not wired into `health.yml`'s per-crate env-gate steps, matching this binary's own e2e-heavy tier (like `client_bundling_cross_pipeline` and its siblings). Dev Self Heal epic #1999, for issue #1826: written RED in Wave 1 (#2000, then `ssr_reload_self_heal_red_e2e.rs` / `red_ssr_only_*_never_reloads_open_tab`) and **inverted in Wave 2 (#2002)**, which landed the shared `ssr_routes_published` signal. An SSR-only project (every route `prerender = false`, no SSG routes at all) now DOES reload an already-open tab after the deferred dev bundle publishes, on BOTH the healthy Cold-lazy boot path (#1182/#1808) and the cold-bootstrap recovery path (#1809): `mark_all_routes_stale` still walks only the SSG `routes_by_source` table and still marks zero routes here, but both channels now call the same `DevRenderSession::note_ssr_routes_published` seam, whose bit reaches `BuildOutcome::ssr_routes_published` and widens `outcome_to_events`'s existing `Page` gate. Each test asserts BOTH halves after a deterministic log-line signal (`"no prebuilt dist/ seed required"` for the healthy path; `"deferred dev bundle failed — cold-lazy"` then `"cold-lazy bootstrap recovered"` for the recovery path, the latter proving a genuine esbuild JSX-syntax failure, not a simulated one): a fresh manual GET returns 200 with the page's marker AND the SSE stream subscribed BEFORE that point delivers a `page` event. **Revert-proof (#2002)**: deleting the boot hook's `session.note_ssr_routes_published()` call reproduces the exact pre-fix symptom in the healthy test ("no SSE event reached the tab … within 30s") while the GET still returns 200 — proof the tests exercise the broadcast, not serving. The recovery seam's own falsifiability is covered at unit level by `healthy_publish_and_cold_bootstrap_recovery_heal_an_ssr_only_tab_identically` (`crates/zfb/src/commands/dev.rs`), which pins that the two paths produce byte-identical `BuildOutcome`s. In the nextest `e2e-heavy` group (flock-adopting via `CrossBinaryE2eLock`), registered in issue #2007 — previously missing from that filter despite spawning `zfb dev`. |
| `crates/zfb/src/commands/build.rs:11853` `bare_package_consume_from_source_sibling_glob_macro_reaches_islands_bundle_unexpanded` | `pending-feature: https://github.com/Takazudo/zudo-front-builder/issues/2090` | Local: `ZFB_ESBUILD_BIN=<abs path> cargo test -p zfb --lib commands::build::tests::bare_package_consume_from_source_sibling_glob_macro_reaches_islands_bundle_unexpanded -- --ignored`. **Deliberately EXCLUDED from CI while RED**: `health.yml`'s "Run zfb command-layer env-gate unit tests (commands::build)" step sweeps this whole module with `commands::build:: -- --ignored`, so it WOULD run this test and go red; that step therefore carries an explicit `--skip bare_package_consume_from_source_sibling_glob_macro_reaches_islands_bundle_unexpanded`. Sub #2090 must DROP that `--skip` in the same commit it swaps the `pending-feature:` tag for `env-gate: esbuild — …`, or the flipped test silently never runs in CI again. Issue #2083 (Staging Correctness 2 epic #2078, Wave 1): RED test for #2048's islands/client silent shape — an ACCEPTED (#2040 declared-entry) consume-from-source sibling reached by BARE PACKAGE NAME is never mirror-enrolled, so its own `import.meta.glob` ships literally, unexpanded, in a GREEN production islands bundle. The host island reaches the sibling via a query-free `require(...)` call — invisible to `collect_import_edges` (guard (a)'s scanner), so `scan_meta.glob_reachable_from_islands` never sees the sibling's glob, the shadow-staging precondition (~build.rs:3732) is never met, and BOTH guard (a) and guard (b) (`stage_escape_audit_policy`, only ever armed when a shadow exists) are skipped entirely — esbuild bundles straight from `project_root` and inlines the sibling's live, unprocessed source. Verified RED: the build returns `Ok` and the emitted bundle contains the literal `import.meta.glob("./data/*.json")` text with no trace of the matched file's content. Scope: nested workspace members only (root-claimed topology is out of scope, matching #2048). Sub #2090 (islands/client acceptance⇒enrolment coupling, or the sanctioned loud-failure fallback) flips this test — replacing the `pending-feature` tag with `env-gate: esbuild — …` per epic #2078's corrected flip protocol, since this is a TRUE env-gate test, not a self-skipping unit test. |

No `flaky:` tagged tests exist as of this audit. The lone `verification:` helper above is a one-time extraction aid, not a regression guard. The `pending-feature:` rows above were all authored by epic #2078's Wave 1 (issues #2079-#2083) and are blocked on later waves of that same epic. Wave 1 left **7**; #2085 (provenance threading, Wave 2) has since flipped and removed its **3**, leaving **4** — #2086 (canonical-key exemption) flips two, #2087 (declared-identity eligibility) one, and #2090 (islands/client coupling) one. **The totals sentence above the table is still an interim Wave 1 measurement and was deliberately NOT re-derived here** — epic #2078's Sub 11 (#2091) owns the authoritative reconciliation once every wave has landed and each `pending-feature` row has been flipped per the epic's corrected flip protocol.

### TS-side flaky/verification idiom (vitest)

No TypeScript/vitest test in this repo has ever needed quarantine — `grep -rn '// flaky:\|// @verification:' --include='*.test.ts' packages/ crates/*/npm` returns nothing as of this writing (issue #1349 confirmed there is nothing to retrofit). This section establishes the convention for the day one does, mirroring the Rust `#[ignore]` taxonomy above. vitest has no first-class "ignore with a machine-readable reason" attribute like Rust's `#[ignore = "..."]`, so the reason lives in a comment directly above `it.skip(...)`:

- **Quarantine a flaky test** — `it.skip(...)` with a `// flaky: <issue-url>` comment immediately above (issue URL mandatory, same rule as Rust's `flaky:`):

  ```ts
  // flaky: https://github.com/Takazudo/zudo-front-builder/issues/9999
  it.skip("retries the SSE reconnect within the 3-attempt budget", async () => {
    // ...
  });
  ```

- **One-time "it was done" proof, not a regression guard** — `it.skip(...)` with a `// @verification: <why>` comment immediately above, mirroring Rust's `verification:` tag (no issue URL needed):

  ```ts
  // @verification: one-time proof the v0.1 -> v0.2 content-schema migration
  // script produced the expected fixture snapshot; not a regression guard.
  it.skip("migrates the v0.1 fixture to the v0.2 schema", async () => {
    // ...
  });
  ```

- The other 3 Rust prefixes (`env-gate:`, `heavy:`, `pending-feature: <issue-url>`) follow the same comment-directly-above-`it.skip(...)` shape if a TS test ever needs them — no precedent yet, so no worked example here.
- **Audit**: `grep -rn '// flaky:\|// @verification:' --include='*.test.ts' packages/ crates/*/npm` — the TS mirror of the Rust manifest's `grep -rn '#\[ignore = "' crates/` rule above.
- Same rules as Rust apply: **never game the gate** (no `it.skip` for `flaky:` without a linked open issue), **quarantine has an exit with a deadline**, and a red check still blocks the author unless it carries a valid `// flaky: <issue-url>` tag (see "Required behavior" item 7 above).
- The first time a real TS test is quarantined or tagged `@verification`, add a manifest table entry here (mirroring the Rust `#[ignore]` manifest table above) so this section stops being purely aspirational.

### T3 cutover manifest (post-release)

What upgrades when zfb ships a stable release, each with an explicit trigger — do not implement any row speculatively; wait for its trigger, then treat this table (not the epic that wrote it) as the up-to-date starting point. The "do not scaffold unused tiers" rule above applies to every row here.

| Item | Current state | Trigger to upgrade |
|---|---|---|
| Weekly → nightly cadence | `exam.yml` (Sat 04:17 UTC) + `drift-net.yml` (Wed 03:43 UTC) run weekly on GitHub-hosted runners only | A stable (non-prerelease) release ships, or external adoption is high enough that a week of undetected drift is unacceptable. Bump both crons to nightly; re-check whether GitHub-hosted runners still fit the budget — still a public repo, so no blacksmith/self-hosted runners are planned by default |
| Windows exam leg | `@takazudo/zfb-win32-x64-msvc` already ships — `release.yml` natively builds it on `windows-latest` every release — but **no CI job ever executes the Rust test suite on Windows**. `health.yml`, `exam.yml`, and `drift-net.yml` are all ubuntu/macOS only | A deliberate commitment to Windows as a *tested* (not just built) platform — e.g. a Windows-specific bug report, or Windows graduating from "we ship a binary" to "we support it." Add a `windows-latest` leg to `exam.yml` mirroring `macos-exam` (full `cargo nextest run --workspace` + doctest re-exam); expect path-separator / file-locking friction the dev-server watcher has never been exercised against |
| wrangler/workerd adapter heavy lane | `packages/zfb-adapter-cloudflare/src/__tests__/d1-binding.test.ts`'s own header comment documents the gap: it uses a synthetic in-memory `D1Database` stub instead of real miniflare/`wrangler dev` because "a real miniflare D1 would test miniflare's SQLite, not this adapter's threading" — the test proves env/binding passthrough, not real D1 SQL semantics or the real Workers runtime lifecycle | The Cloudflare adapter becomes a primary supported deploy target (vs. today's acceptance-test-only confidence level), or a bug surfaces that the stub provably cannot catch. Add a `wrangler dev`/miniflare-backed integration lane — almost certainly T3/T4 (it boots a real Workers runtime), not T1 |
| Homebrew automation + verify | `scripts/update-homebrew-formula.sh vX.Y.Z --push` exists and works against `Takazudo/homebrew-tap`, but is a **manual** step run by hand after a stable release publishes (see `RELEASE_DAY_CHECKLIST.md` and `l-make-release` SKILL.md Step 11) — no CI automation, no post-update `brew install` verification | Homebrew installs are used often enough in practice to justify automating the tap push at the end of `release.yml` (stable releases only), plus a smoke step that actually `brew install`s and runs `zfb --version` against the freshly-pushed formula |
| linux-musl decision | `packages/zfb/bin/detect-musl.mjs` (wired into `packages/zfb/bin/zfb.mjs`) actively detects Alpine/musl and fails with a friendly "musl/Alpine is not supported yet — no `@takazudo/zfb-linux-*-musl` package exists" error rather than crashing opaquely on a glibc-only binary. No musl package exists under `packages/`, and `release.yml` has no musl build leg | Real demand surfaces (e.g. an issue asking for Alpine/musl support). Then decide whether to add `@takazudo/zfb-linux-x64-musl` (cross-compiled, a new `release.yml` matrix leg, a new `optionalDependencies` entry) or keep the friendly-error posture permanently — this is a product decision, not just a CI one |
| vitest coverage reporting | No `vitest.config.ts`/`.mjs` in the workspace (`packages/zfb`, `packages/zfb-runtime`, `packages/zfb-adapter-cloudflare`, `packages/create-zfb`, `crates/zfb-islands/npm`) configures a `coverage:` block — `pnpm -r test` runs vitest with zero coverage instrumentation today | Coverage becomes a stated release-readiness gate (e.g. a pre-1.0 checklist item). Add `@vitest/coverage-v8` per package and a `coverage: { provider: "v8", ... }` block to each `vitest.config.*`, and explicitly decide whether thresholds gate T1 (`health.yml`) or stay a T4/local-only signal — do not wire a hard coverage-% gate into `health.yml` without that decision being made on purpose |

### `b4push` — local pre-push pass (T4)

`pnpm b4push` (`scripts/run-b4push.sh`) runs a **bounded** fast pass before pushing, cheap → expensive: shell-script syntax (`bash -n`), the offline `tests/unit/*.sh` unit tests (actually executed — one step per file, not just parsed), `cargo fmt --check`, `pnpm format:check`, `pnpm -r --if-present typecheck`, `pnpm -r test`, and `cargo clippy -D warnings` (warm tree). It prints a per-step duration summary at the end. It is **not** the authoritative gate — `health.yml` is. b4push is the *fast subset*; the full Rust test suite is too heavy for a pre-push loop by default (V8 first-compile is 15–30 min).

- Escapes: `B4PUSH_SKIP_CLIPPY=1`, `B4PUSH_SKIP_JS_TEST=1`.
- Heavy opt-in: `B4PUSH_FULL=1 pnpm b4push` additionally runs the **full health.yml parity set** (issue #1332) — not just the workspace test suite: `cargo nextest run --workspace` (or `cargo test --workspace` when nextest is absent) using nextest's **default** profile (`retries = 0`, unlike CI's `--profile ci`); `cargo test --workspace --doc` (nextest branch only — nextest doesn't run doctests); the `zfb-md-extras` `test-utils`-gated suite plus its scoped `cargo clippy`; `cargo check --no-default-features -p zfb --tests`; the 16-test `zfb-islands` esbuild env-gate suite; #1504's real-zfb cross-pipeline acceptance test; and the three command/package locations carrying tailwindcss-v4 env-gates. The command-layer step pins both esbuild and Tailwind because it co-runs two esbuild tests and one Tailwind test. These **5 env-gate steps** skip (rather than fail) when their required staged binary slot is absent; run `cargo build --workspace --all-targets` first for full parity.
- It is **not** wired into the git `pre-push` hook (that hook only enforces the worktree-push policy above) — run it manually before pushing.
